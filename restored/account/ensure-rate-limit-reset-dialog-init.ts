// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `iOs` / export `Iv` — empty __esm before rate-limit reset dialog (nothingToReset messages).

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureRateLimitResetDialogInit(): void {}
