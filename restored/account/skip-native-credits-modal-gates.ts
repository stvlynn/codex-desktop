// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Statsig layer / params that skip the native credits modal (`JTs` / export `oy` init).

/** Combined-modal / auto-reload discount gate (`UTs`). */
export const COMBINED_CREDITS_MODAL_FEATURE_GATE_ID = "1721641661";

/** Layer that can skip opening the native credits modal (`GTs`). */
export const SKIP_NATIVE_CREDITS_MODAL_LAYER_ID = "1112993408";

/** Always skip native modal (`KTs`). */
export const SKIP_NATIVE_MODAL_ALWAYS_PARAM = "skip_native_modal_always";

/** Skip native modal when the user has no saved auto-reload (`qTs`). */
export const SKIP_NATIVE_MODAL_IF_NO_AUTO_RELOAD_PARAM =
  "skip_native_modal_if_no_auto_reload";

/** Debounce / settle delay near the credits modal opener (`WTs`). */
export const CREDITS_MODAL_SETTLE_DELAY_MS = 2000;
