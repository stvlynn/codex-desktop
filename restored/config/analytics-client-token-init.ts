// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Analytics client token ESM init (`Meu` / export `gr`) — pairs with `hr`.

import { ANALYTICS_CLIENT_TOKEN } from "../config/analytics-client-token";

export { ANALYTICS_CLIENT_TOKEN };

/** Bundle export `gr` — Rolldown ESM init retained as no-op. */
export function ensureAnalyticsClientTokenInit(): void {}
