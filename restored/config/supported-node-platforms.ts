// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `Ixe` / export `Omt`).

export const SUPPORTED_NODE_PLATFORMS = ["darwin", "linux", "win32"] as const;
export type SupportedNodePlatform = (typeof SUPPORTED_NODE_PLATFORMS)[number];
