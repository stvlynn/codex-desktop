// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `AE` / export `P4`).

export const USER_CONFIG_PATH = ["config", "user"] as const;
export type UserConfigPath = (typeof USER_CONFIG_PATH)[number];
