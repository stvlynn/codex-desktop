// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `ME` / export `N4`).

export const MCP_SERVERS_STATUS_PATH = ["mcp", "servers", "status"] as const;
export type McpServersStatusPath = (typeof MCP_SERVERS_STATUS_PATH)[number];
