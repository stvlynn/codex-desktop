// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `viu` / export `Un`).

export const APP_HOST_UPDATE_STATUS_PATH = [
  "app-host",
  "primary-runtime",
  "update-status",
] as const;
export type AppHostUpdateStatusPath =
  (typeof APP_HOST_UPDATE_STATUS_PATH)[number];
