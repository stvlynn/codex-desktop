// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `Crr` / export `Q0`).

export const MEMORIES_ID = "memories";

/** No-op Rolldown ESM init that assigned `Crr` (export `e2` / bundle `wrr`). */
export function ensureMemoriesIdInit(): void {}
