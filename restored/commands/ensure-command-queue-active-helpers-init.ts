// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `Yul` / export `Nc` — empty __esm before command-queue isActive / completed helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureCommandQueueActiveHelpersInit(): void {}
