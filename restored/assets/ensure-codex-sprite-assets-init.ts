// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `a5l` / export `Fr` — fan-in composer Utt before codex sprite assetRef catalog helpers.

import { ensureComposerEsm_Utt_Init } from "../composer/composer-esm-inits";

/** Fan-in ensure-init shim (bundle body only calls mapped ensure*Init peers). */
export function ensureCodexSpriteAssetsInit(): void {
  ensureComposerEsm_Utt_Init();
}
