// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `slu` / export `tn` — empty __esm before path separator / UNC / drive-letter detect.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensurePathSeparatorDetectInit(): void {}
