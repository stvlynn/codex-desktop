// Restored from ref/webview/assets/app-initial-C-fROkKo.js (inlined lodash cloneDeep guts)
//
// npm-shim: lodash internal helpers used by cloneDeep CJS interop. Consumers call
// `alias()` to obtain the real function/constructor (bundle `yo()`/`Wa()` pattern).

import lodashAssignValue from "lodash/_assignValue";
import lodashBaseFor from "lodash/_baseFor";
import lodashBaseRest from "lodash/_baseRest";
import lodashCloneBuffer from "lodash/_cloneBuffer";
import lodashCloneTypedArray from "lodash/_cloneTypedArray";
import lodashCopyArray from "lodash/_copyArray";
import lodashCopyObject from "lodash/_copyObject";
import lodashInitCloneObject from "lodash/_initCloneObject";
import lodashIsIterateeCall from "lodash/_isIterateeCall";
import LodashListCache from "lodash/_ListCache";

/** Bundle `nPc` / export `Df` — lodash `_initCloneObject`. */
export function initCloneObject(): typeof lodashInitCloneObject {
  return lodashInitCloneObject;
}

/** Bundle `$Nc` / export `Of` — lodash `_cloneTypedArray`. */
export function cloneTypedArray(): typeof lodashCloneTypedArray {
  return lodashCloneTypedArray;
}

/** Bundle `dcc` / export `Zf` — lodash `_copyArray`. */
export function copyArray(): typeof lodashCopyArray {
  return lodashCopyArray;
}

/** Bundle `qme` / export `Mgt` — lodash `_assignValue`. */
export function assignValue(): typeof lodashAssignValue {
  return lodashAssignValue;
}

/** Bundle `LNc` / export `jf` — lodash `_copyObject`. */
export function copyObject(): typeof lodashCopyObject {
  return lodashCopyObject;
}

/** Bundle `UNc` / export `kf` — lodash `_cloneBuffer`. */
export function cloneBuffer(): typeof lodashCloneBuffer {
  return lodashCloneBuffer;
}

/** Bundle `_o` / export `K_t` — lodash `ListCache` constructor factory. */
export function ListCache(): typeof LodashListCache {
  return LodashListCache;
}

/** Bundle `qZr` / export `vY` — lodash `_baseFor`. */
export function baseFor(): typeof lodashBaseFor {
  return lodashBaseFor;
}

/** Bundle `AXi` / export `GB` — lodash `_baseRest`. */
export function baseRest(): typeof lodashBaseRest {
  return lodashBaseRest;
}

/** Bundle `jXi` / export `WB` — lodash `_isIterateeCall`. */
export function isIterateeCall(): typeof lodashIsIterateeCall {
  return lodashIsIterateeCall;
}
