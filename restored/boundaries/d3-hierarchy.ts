// Restored from ref/webview/assets/src-DWyvtO7a.js — vendored d3-hierarchy + d3-scale-chromatic
//
// npm-shim: Mermaid/D3 hierarchy+treemap chunk plus categorical color schemes
// (Tableau10 / Set2). Letter aliases match Rolldown export tokens; `t`/`u`/…
// are chunk ESM-init stubs.

export {
  cluster,
  hierarchy,
  pack,
  partition,
  stratify,
  tree,
  treemap,
  treemapBinary,
  treemapDice,
  treemapResquarify,
  treemapSlice,
  treemapSliceDice,
  treemapSquarify,
  // Rolldown letter aliases used by workbook-runtime / diagram consumers
  treemapSquarify as a,
  hierarchy as c,
  partition as m,
  treemap as r,
  tree as s,
} from "d3-hierarchy";

export {
  schemeSet2,
  schemeTableau10,
  schemeSet2 as d,
  schemeTableau10 as f,
  schemeTableau10 as p,
} from "d3-scale-chromatic";

/** Bundle ESM-init stubs matching leftover letter export tokens. */
export function i(): void {}
export function l(): void {}
export function n(): void {}
export function o(): void {}
export function t(): void {}
export function u(): void {}
