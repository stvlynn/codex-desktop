// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `pwa` / export `NF`.

export function isWaitingOrWorkingStatus(status: string): boolean {
  return status === "waiting" || status === "working";
}
