// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `hAl` / export `cc` — empty __esm before turn helpers cluster.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureTurnHelpersInit(): void {}
