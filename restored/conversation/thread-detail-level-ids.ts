// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Thread detail / work-mode level literals (`nS` / `$ln`).

/** Everyday / prose thread detail level (`nS` / export `N7`). */
export const THREAD_DETAIL_LEVEL_STEPS_PROSE = "STEPS_PROSE";

/** Coding / commands thread detail level (`$ln` / export `M7`). */
export const THREAD_DETAIL_LEVEL_STEPS_COMMANDS = "STEPS_COMMANDS";
