// Restored from ref/webview/assets/pull-request-route-CROLmqPD.js
// Wave5 stub-pass soft companions.
const DeferredUiYO2: any = undefined;
const _t: any = undefined;
const deferredConversationZp: any = undefined;
const deferredNavigationEN: any = undefined;
const ensureCloseGlyphIconInit: any = undefined;
const remoteHostedPipHiddenThreadIds: any = undefined;
const useClearBrowserBrowsingDataMutation: any = undefined;

// Wave FZ — full polished body from `pull-request-route-CROLmqPD/auto-polished.tsx` with companion import rewrite.
// Soft-deferred host replaced; NOT app-initial extractFn / promote / producer barrel.
// Residual companion stubs: 39 (verified 78/116).
// Wave5d — FZ repair from fz-pascal L=2280 sus=32; JSX PascalCase + careful split.
// Wave5d careful split 2/4
/* split-lane-import-depth:1 */

import { CodexBrowserSurfaceActionType } from "../../analytics/codex-browser-surface-action-type";
import { CodexPluginActionResult } from "../../analytics/codex-plugin-action-result";
import { CodexPluginActionType } from "../../analytics/codex-plugin-action-type-enum";
import { CodexPluginDirectoryEntrypoint } from "../../analytics/codex-plugin-directory-entrypoint";
import { CodexReferralInviteModalBackendErrorType } from "../../analytics/codex-referral-invite-modal-backend-error-type";
import { nuxAccessModeStub } from "../../appgen/nux-access-mode-stub";
import { useAppgenScreenshotDataUrlQuery } from "../../apps/use-appgen-screenshot-data-url-query";
import { decayZoomPercent } from "../../artifact/clamp-zoom-percent";
import {
  appScopeAtom,
  ensureAppScopeInit,
} from "../../runtime/app-scope-runtime";
import {
  ensureComposerEsm_FH_Init,
  ensureComposerEsm_Hlt_Init,
  ensureComposerEsm_M0_Init,
  ensureComposerEsm_MT_Init,
  ensureComposerEsm_P5_Init,
  ensureComposerEsm_S8_Init,
  ensureComposerEsm_Sut_Init,
} from "../../boundaries/composer-esm-inits";
import { ensureConversationPageEsm_B0_Init } from "../../conversation/conversation-page-esm-inits";
import {
  react,
  reactCompilerRuntime,
} from "../../boundaries/react-cjs-runtime";
import {
  Navigate,
  useLocation,
  useNavigate,
} from "../../boundaries/react-router-navigation";
import { conversationsSidebarMessages } from "../../chatgpt/conversations-sidebar-messages";
import { composerNavigation } from "../../composer/composer-navigation";
import { INLINE_MENTION_GROUP_CLASS } from "../../composer/inline-mention-class-names";
import { WritingBlockRenderer } from "../../composer/writing-block-renderer";
import { VSCODE_EDITOR_ID } from "../../config/vscode-editor-id";
import { DiffLinesStatsBadge } from "../../diff/diff-lines-stats-badge";
import { pullRequestLastAccount } from "../../git/pull-request-last-account-2";
import { usePlatformAwareFeatureGate as UsePlatformAwareFeatureGate } from "../../hooks/use-platform-aware-feature-gate";
import { useRouteParams } from "../../hooks/use-route-params";
import { hostConfig2 } from "../../hosts/host-config2";
import { MemoizedFormattedMessage } from "../../i18n/memoized-formatted-message";
import { ensureIntlFormattersInit, useIntl } from "../../i18n/use-intl";
import { AppIconAZ } from "../../icons/app-icon-az";
import { AppIconJO } from "../../icons/app-icon-jo";
import { AppIconKG } from "../../icons/app-icon-kg";
import { AppIconQG } from "../../icons/app-icon-qg";
import { AppIconRo } from "../../icons/app-icon-ro";
import { AppIconYm } from "../../icons/app-icon-ym";
import { AppIconZlt } from "../../icons/app-icon-zlt";
import { setVacRouteForWorkspacePath } from "../../navigation/set-vac-route-for-workspace-path";
import { commonJsInit, esmInit } from "../../runtime/rolldown-runtime";
import { gpuTearingDebugSettingsAtom } from "../../settings/gpu-tearing-debug-settings";
import { ensureSettingsQueryAtomsInit } from "../../settings/settings-ipc";
import { closeSidePanelUnlessWindowResize } from "../../ui/close-side-panel-unless-window-resize";
import { CopyButton } from "../../ui/copy-button";
import { DropdownMenu, ensureDropdownMenuInit } from "../../ui/dropdown-menu";
import {
  DropdownMenuPopover,
  ensureDropdownMenuPopoverInit,
} from "../../ui/dropdown-menu-popover";
import { OptionalTooltip } from "../../ui/optional-tooltip";
import { SearchableDetailPageLayout } from "../../ui/searchable-detail-page-layout";
import {
  ensureSelectableListRowActivationInit,
  ensureSelectableListRowInit as EnsureSelectableListRowInit,
} from "../../ui/selectable-list-row";
import { composeEventHandlers } from "../../utils/compose-event-handlers";
import { extractColonPrefixedNamedTokens } from "../../utils/extract-colon-prefixed-named-tokens";
import { parseCachedVacValue } from "../../utils/parse-cached-vac-value";
import { ensureImportSettingsCLInit } from "../../utils/wave-as-gap-ensure-inits";
import { filterConversationTimelineItems } from "../filter-conversation-timeline-items";
import { hostnameFromPullRequestViewState } from "../hostname-from-pull-request-view-state";
import {
  pullRequestDetailJ,
  pullRequestDetailK,
  pullRequestDetailR,
  pullRequestDetailUpperA,
  pullRequestDetailUpperD,
  pullRequestDetailUpperF,
  pullRequestDetailUpperM,
  pullRequestDetailUpperN as PullRequestDetailUpperN,
  pullRequestDetailUpperO,
  pullRequestDetailUpperP,
  pullRequestDetailUpperT as PullRequestDetailUpperT,
} from "../pull-request-detail-query";
import { resolvePullRequestHostId } from "../resolve-pull-request-host-id";

// Wave5d soft JSX companions.
function CommitModal(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}
function Dismiss(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}
function EnsureEnvironmentLabelsSDInit(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}
function Alpha(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}
function Bravo(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}
function ReadLoginRouteQuerySnapshot(
  props: {
    children?: unknown;
    [key: string]: unknown;
  } = {},
): unknown {
  return props.children ?? null;
}

// Wave5d soft stubs.
const AppInitialAD: any = undefined;
const AppInitialC: any = undefined;
const AppInitialEm: any = undefined;
const AppInitialID: any = undefined;
const AppInitialJm: any = undefined;
const AppInitialLD: any = undefined;
const AppInitialLj: any = undefined;
const AppInitialMm: any = undefined;
const AppInitialPm: any = undefined;
const AppInitialUj: any = undefined;
const PullRequestRouteHelper11: any = undefined;
const PullRequestRouteHelper7: any = undefined;
const PullRequestRouteHelper8: any = undefined;
const PullRequestRouteHelper9: any = undefined;
const deferredUiCm: any = undefined;
const copper: any = undefined;
const delta: any = undefined;
const echo: any = undefined;
const falcon: any = undefined;
const gamma: any = undefined;
const harbor: any = undefined;
const indigo: any = undefined;
const jade: any = undefined;
const kite: any = undefined;
const lemon: any = undefined;
const marble: any = undefined;
const nickel: any = undefined;
const onyx: any = undefined;
const pearl: any = undefined;
const quartz: any = undefined;
const river: any = undefined;
const slate: any = undefined;
const timber: any = undefined;
const umbra: any = undefined;
const violet: any = undefined;
const willow: any = undefined;
const xenon: any = undefined;
const yellow: any = undefined;
const zinc: any = undefined;
const amber: any = undefined;
const basalt: any = undefined;
const cedar: any = undefined;
const daisy: any = undefined;
const ember: any = undefined;
const flint: any = undefined;
const garnet: any = undefined;
const hazel: any = undefined;
const ivory: any = undefined;
const jasper: any = undefined;
const kelp: any = undefined;
const lotus: any = undefined;
const mint: any = undefined;
const nova: any = undefined;
const olive: any = undefined;
const prism: any = undefined;
const quill: any = undefined;
const reef: any = undefined;
const sage: any = undefined;
const topaz: any = undefined;
const ultra: any = undefined;
const vapor: any = undefined;
const wheat: any = undefined;
const yarn: any = undefined;
const zephyr: any = undefined;
const acorn: any = undefined;
const bloom: any = undefined;
const coral: any = undefined;
const drift: any = undefined;
const eagle: any = undefined;
const frost: any = undefined;
const glide: any = undefined;
const honey: any = undefined;
const iris: any = undefined;
const jewel: any = undefined;
const knoll: any = undefined;
function lunar(vale) {
  let {
      account,
      error,
      effectiveRepository,
      hasNextPage,
      isLoading,
      isLoadMoreDisabled,
      isLoadingMore,
      isPartial,
      selectedPullRequest,
      sections,
      onLoadMore,
      onRetry,
    } = vale,
    wave = AppInitialJm(account);
  let apex = wave,
    brook = CodexBrowserSurfaceActionType(pullRequestDetailJ, apex),
    cliff = CodexBrowserSurfaceActionType(pullRequestDetailUpperO, apex),
    dusk = sections.some(moss);
  let elm = dusk;
  if (isLoading && !elm) {
    let hill;
    return (
      <AppIconYm
        {...{
          density: "compact",
          loadingLabel: (
            <MemoizedFormattedMessage
              {...{
                id: "codex.pullRequests.inbox.loading",
                defaultMessage: "Loading pull requests",
                description: "Accessible status while pull requests load",
              }}
            />
          ),
        }}
      />
    );
  }
  if (error != null && !elm) {
    let isle = (
      <MemoizedFormattedMessage
        {...{
          id: "codex.pullRequests.inbox.retry",
          defaultMessage: "Try again",
          description: "Action to retry loading pull requests",
        }}
      />
    );
    let juniper = (
      <ReadLoginRouteQuerySnapshot
        {...{
          color: "secondary",
          onClick: onRetry,
          children: isle,
        }}
      />
    );
    let lagoon = (
      <MemoizedFormattedMessage
        {...{
          id: "codex.pullRequests.inbox.error",
          defaultMessage: "Unable to load pull requests",
          description: "Error title when the pull request inbox cannot load",
        }}
      />
    );
    let meadow;
    return (
      <Dismiss
        {...{
          className: "min-h-64 flex-1",
          actions: juniper,
          description: error,
          title: lagoon,
        }}
      />
    );
  }
  let fern = isPartial ? (
    <AppInitialLj align="start" className="gap-2">
      <span role="status">
        {
          <MemoizedFormattedMessage
            {...{
              id: "codex.pullRequests.inbox.partial",
              defaultMessage: "Some pull requests couldn’t be loaded",
              description:
                "Notice shown when one pull request search stream fails",
            }}
          />
        }
      </span>
      {
        <ReadLoginRouteQuerySnapshot
          {...{
            color: "secondary",
            disabled: isLoadingMore,
            size: "toolbar",
            onClick: onRetry,
            children: (
              <MemoizedFormattedMessage
                {...{
                  id: "codex.pullRequests.inbox.retryPartial",
                  defaultMessage: "Retry",
                  description:
                    "Action to retry a partially loaded pull request inbox",
                }}
              />
            ),
          }}
        />
      }
    </AppInitialLj>
  ) : null;
  let grove = elm ? (
    <div className="flex flex-col gap-4">
      {sections.map((item) =>
        item.items.length === 0 ? null : (
          <section key={item.key} className="flex flex-col gap-0.5">
            <h2 className="px-3 pb-1 text-xs font-medium text-token-description-foreground">
              {
                <PullRequestRouteHelper9
                  {...{
                    section: item.key,
                  }}
                />
              }
            </h2>
            <div className="flex flex-col gap-0.5">
              {item.items.map((_item) => (
                <PullRequestRouteHelper7
                  key={_item.nodeId}
                  {...{
                    account,
                    pullRequest: _item,
                    selectedPullRequest,
                    showAuthorAvatar: item.key !== "authored",
                    showRepository: effectiveRepository == null,
                  }}
                />
              ))}
            </div>
          </section>
        ),
      )}
      {hasNextPage ? (
        <ReadLoginRouteQuerySnapshot
          {...{
            className: "self-center",
            color: "secondary",
            disabled: isLoadMoreDisabled,
            loading: isLoadingMore,
            onClick: onLoadMore,
            children: (
              <MemoizedFormattedMessage
                {...{
                  id: "codex.pullRequests.inbox.loadMore",
                  defaultMessage: "Load more",
                  description: "Action to load the next page of pull requests",
                }}
              />
            ),
          }}
        />
      ) : null}
    </div>
  ) : (
    <Dismiss
      {...{
        className: "min-h-64 flex-1",
        spacing: "compact",
        tone: "faded",
        title:
          brook.trim().length > 0 ? (
            <MemoizedFormattedMessage
              {...{
                id: "codex.pullRequests.inbox.emptySearch",
                defaultMessage: "No pull requests match this search",
                description:
                  "Empty state when no pull requests match the text filter",
              }}
            />
          ) : cliff === "reviewing" ? (
            <MemoizedFormattedMessage
              {...{
                id: "codex.pullRequests.inbox.emptyReviewing",
                defaultMessage: "You’re all caught up",
                description:
                  "Empty state when there are no open pull requests to review",
              }}
            />
          ) : (
            <MemoizedFormattedMessage
              {...{
                id: "codex.pullRequests.inbox.empty",
                defaultMessage: "No pull requests found",
                description:
                  "Empty state when no pull requests match the selected filters",
              }}
            />
          ),
      }}
    />
  );
  return (
    <>
      {fern}
      {grove}
    </>
  );
}
function moss(nest) {
  return nest.items.length > 0;
}
function north(oak) {
  let {
      account,
      pullRequest,
      selectedPullRequest,
      showAuthorAvatar,
      showRepository,
    } = oak,
    petal = CodexPluginActionType(ensureComposerEsm_S8_Init),
    quiet = useNavigate(),
    rain =
      selectedPullRequest != null &&
      AppInitialMm(selectedPullRequest) ===
        AppInitialMm(pullRequest.pullRequest);
  let seed = rain,
    trail = `${pullRequest.pullRequest.owner}/${pullRequest.pullRequest.repository}`,
    urn =
      pullRequest.authorAvatarUrl ??
      remoteHostedPipHiddenThreadIds(pullRequest.authorLogin, 32);
  let vine = urn,
    wind = seed ? "true" : undefined,
    yarrow = (
      <div className="relative flex size-5 items-center justify-center">
        {
          <PullRequestRouteHelper8
            {...{
              pullRequest,
            }}
          />
        }
      </div>
    );
  let azure = () => {
    if (seed) {
      if (!petal.get(ensureComposerEsm_M0_Init)) {
        closeSidePanelUnlessWindowResize(petal, true);
        return;
      }
      composeEventHandlers(petal, account);
      quiet("/pull-requests", {
        state: deferredConversationZp(account),
      });
      return;
    }
    quiet(AppInitialEm(pullRequest.pullRequest), {
      state: deferredConversationZp(account),
    });
  };
  let birch = (
    <INLINE_MENTION_GROUP_CLASS
      {...{
        dateString: pullRequest.updatedAt,
      }}
    />
  );
  let canyon = showAuthorAvatar ? (
    vine == null ? (
      <UsePlatformAwareFeatureGate
        {...{
          className: "size-4 shrink-0",
        }}
      />
    ) : (
      <img
        alt=""
        className="size-4 shrink-0 rounded-full bg-white"
        src={vine}
      />
    )
  ) : null;
  let dew = showRepository ? (
    <span className="max-w-[35%] shrink-0 truncate">{trail}</span>
  ) : null;
  let ever = (
    <span className="min-w-0 flex-1 truncate">{pullRequest.headBranch}</span>
  );
  let field = (
    <div className="flex min-w-0 items-center gap-2">
      {canyon}
      {dew}
      {ever}
    </div>
  );
  let grain = pullRequest.hasDiffStats ? (
    <DiffLinesStatsBadge
      {...{
        linesAdded: pullRequest.additions,
        linesRemoved: pullRequest.deletions,
        variant: "monochrome",
      }}
    />
  ) : null;
  return (
    <EnsureSelectableListRowInit
      {...{
        ariaCurrent: wind,
        ariaLabel: pullRequest.title,
        density: "compact",
        icon: yarrow,
        isSelected: seed,
        onSelect: azure,
        rightText: birch,
        secondLine: field,
        secondLineRightText: grain,
        title: pullRequest.title,
      }}
    />
  );
}
function orbit(haven) {
  let { pullRequest } = haven;
  if (pullRequest.state !== "open") {
    let leaf;
    return (
      <EnsureEnvironmentLabelsSDInit
        {...{
          className: "size-5 shrink-0",
          status: pullRequest.state,
        }}
      />
    );
  }
  let ink = pullRequest.mergeBlocker === "conflicts",
    jadeite = pullRequest.isDraft ? "draft" : "open",
    kernel = AppInitialID({
      canMerge: pullRequest.canMerge,
      ciStatus: pullRequest.ciStatus,
      hasMergeConflicts: ink,
      status: jadeite,
    });
  return (
    <CommitModal
      {...{
        className: "size-5 shrink-0",
        state: kernel,
      }}
    />
  );
}
function pine(maple) {
  let { section } = maple;
  switch (section) {
    case "review_requested": {
      let nimbus;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.section.reviewRequested",
            defaultMessage: "Review requested",
            description:
              "Heading for pull requests requesting the active user’s review",
          }}
        />
      );
    }
    case "reviewed": {
      let opal;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.section.reviewed",
            defaultMessage: "Previously reviewed",
            description: "Heading for previously reviewed pull requests",
          }}
        />
      );
    }
    case "authored": {
      let plume;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.section.authored",
            defaultMessage: "Authored",
            description:
              "Heading for pull requests authored by the active user",
          }}
        />
      );
    }
    case "results": {
      let quillow;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.section.results",
            defaultMessage: "Results",
            description:
              "Heading for pull requests matching an explicit relationship filter",
          }}
        />
      );
    }
  }
}
function quest(root) {
  let {
      lifecycle,
      repositories,
      repository,
      statusDisabled,
      onLifecycleChange,
      onRepositoryChange,
    } = root,
    silk = useIntl(),
    thorn = repository != null || lifecycle !== "open",
    upland = silk.formatMessage({
      id: "codex.pullRequests.inbox.filters",
      defaultMessage: "Filter pull requests",
      description: "Accessible label for the pull request filter menu",
    });
  let vista = thorn ? (
    <Alpha
      {...{
        className: "icon-sm",
      }}
    />
  ) : (
    <Bravo
      {...{
        className: "icon-sm",
      }}
    />
  );
  let wisp = (
    <ReadLoginRouteQuerySnapshot
      {...{
        "aria-label": upland,
        color: "secondary",
        size: "toolbar",
        uniform: true,
        children: vista,
      }}
    />
  );
  let yonder, zenith;
  yonder = (
    <MemoizedFormattedMessage
      {...{
        id: "codex.pullRequests.inbox.statusFilter",
        defaultMessage: "Status",
        description: "Menu item that opens pull request status filters",
      }}
    />
  );
  zenith = ["all", "open", "merged", "closed"];
  let anvil = zenith.map((item) => (
    <DropdownMenu.Item
      key={item}
      {...{
        RightIcon: lifecycle === item ? AppIconZlt : undefined,
        onSelect: () => {
          onLifecycleChange(item);
        },
        children: (
          <PullRequestRouteHelper11
            {...{
              lifecycle: item,
            }}
          />
        ),
      }}
    />
  ));
  let beacon = (
    <DropdownMenu.FlyoutSubmenuItem
      {...{
        disabled: statusDisabled,
        LeftIcon: CodexReferralInviteModalBackendErrorType,
        label: yonder,
        children: anvil,
      }}
    />
  );
  let crag = (
    <MemoizedFormattedMessage
      {...{
        id: "codex.pullRequests.inbox.repositoryFilter",
        defaultMessage: "Repository",
        description: "Menu item that opens pull request repository filters",
      }}
    />
  );
  let dome = repository == null ? AppIconZlt : undefined,
    eddy = () => {
      onRepositoryChange(null);
    };
  let fjord = (
    <MemoizedFormattedMessage
      {...{
        id: "codex.pullRequests.inbox.repository.all",
        defaultMessage: "All repositories",
        description: "Option to show pull requests from all repositories",
      }}
    />
  );
  let glen = (
    <DropdownMenu.Item
      {...{
        RightIcon: dome,
        onSelect: eddy,
        children: fjord,
      }}
    />
  );
  let hearth;
  {
    let jetty;
    jetty = (knob) => {
      let ledge =
        repository?.owner === knob.repository.owner &&
        repository.repository === knob.repository.repository;
      return (
        <DropdownMenu.Item
          key={knob.label}
          {...{
            RightIcon: ledge ? AppIconZlt : undefined,
            onSelect: () => {
              onRepositoryChange(knob.repository);
            },
            children: <span className="truncate">{knob.label}</span>,
          }}
        />
      );
    };
    hearth = repositories.map(jetty);
  }
  let inlet = (
    <DropdownMenu.FlyoutSubmenuItem
      {...{
        LeftIcon: AppIconRo,
        contentClassName: "max-w-80",
        label: crag,
        children: [glen, hearth],
      }}
    />
  );
  return (
    <DropdownMenuPopover
      {...{
        align: "end",
        contentWidth: "menuNarrow",
        side: "bottom",
        triggerButton: wisp,
        children: [beacon, inlet],
      }}
    />
  );
}
function ridge(mire) {
  let { lifecycle } = mire;
  switch (lifecycle) {
    case "all": {
      let nook;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.lifecycle.all",
            defaultMessage: "All states",
            description: "Option to show pull requests in every state",
          }}
        />
      );
    }
    case "open": {
      let oxbow;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.lifecycle.open",
            defaultMessage: "Open",
            description: "Option to show open pull requests",
          }}
        />
      );
    }
    case "merged": {
      let pond;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.lifecycle.merged",
            defaultMessage: "Merged",
            description: "Option to show merged pull requests",
          }}
        />
      );
    }
    case "closed": {
      let quarry;
      return (
        <MemoizedFormattedMessage
          {...{
            id: "codex.pullRequests.inbox.lifecycle.closed",
            defaultMessage: "Closed",
            description: "Option to show closed pull requests",
          }}
        />
      );
    }
  }
}
var storm,
  tide,
  unity = esmInit(() => {
    storm = reactCompilerRuntime();
    ensureComposerEsm_MT_Init();
    ensureIntlFormattersInit();
    ensureComposerEsm_P5_Init();
    ensureConversationPageEsm_B0_Init();
    ensureSettingsQueryAtomsInit();
    decayZoomPercent();
    ensureDropdownMenuInit();
    ensureImportSettingsCLInit();
    AppInitialUj();
    useClearBrowserBrowsingDataMutation();
    deferredUiCm();
    WritingBlockRenderer();
    ensureSelectableListRowActivationInit();
    nuxAccessModeStub();
    ensureComposerEsm_Sut_Init();
    ensureComposerEsm_Hlt_Init();
    gpuTearingDebugSettingsAtom();
    conversationsSidebarMessages();
    DeferredUiYO2();
    AppInitialC();
    CodexPluginDirectoryEntrypoint();
    ensureDropdownMenuPopoverInit();
    _t();
    knoll();
    ensureCloseGlyphIconInit();
    AppIconQG();
    deferredNavigationEN();
    AppInitialLD();
    AppInitialAD();
    filterConversationTimelineItems();
    ensureComposerEsm_FH_Init();
    AppInitialPm();
    pullRequestLastAccount();
    pullRequestDetailUpperD();
  });
