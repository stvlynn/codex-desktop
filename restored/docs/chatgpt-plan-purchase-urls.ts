// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Personal plan checkout URLs used by plan-change helpers (`iEs`–`sEs` near `cQ`).

/** Bundle `iEs` (co-located with export `ty` / `nEs`). */
export const CHATGPT_COM_CODEX_PURCHASE_PLUS_URL =
  "https://chatgpt.com/codex/purchase/plus?checkout_from=codex_app";

/** Bundle `aEs` (co-located with export `ty` / `nEs`). */
export const CHATGPT_COM_CODEX_PURCHASE_GO_URL =
  "https://chatgpt.com/codex/purchase/go?checkout_from=codex_app";

/** Bundle `oEs` (co-located with export `ty` / `nEs`). */
export const CHATGPT_COM_CODEX_PURCHASE_PRO5X_URL =
  "https://chatgpt.com/codex/purchase/pro5x?checkout_from=codex_app";

/** Bundle `sEs` (co-located with export `ty` / `nEs`). */
export const CHATGPT_COM_CODEX_PURCHASE_PRO_URL =
  "https://chatgpt.com/codex/purchase/pro?checkout_from=codex_app";
