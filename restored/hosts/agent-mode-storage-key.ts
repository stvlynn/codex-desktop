// Restored from ref/webview/assets/app-initial-C-fROkKo.js

/** Persisted map key for per-host agent mode (bundle `ime` / `Ggt`). */
export const AGENT_MODE_BY_HOST_ID_STORAGE_KEY = "agent-mode-by-host-id";
