// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `HSu` / export `y`.

export const CHRONICLE_APP_ID = "chronicle";

/** No-op Rolldown ESM init that assigned `HSu` (export `b` / bundle `USu`). */
export function ensureChronicleAppIdInit(): void {}
