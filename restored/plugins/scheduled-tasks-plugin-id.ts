// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Literal constant (bundle `P4i` / export `Pz`).

export const SCHEDULED_TASKS_PLUGIN_ID = ["plugin-scheduled-tasks"] as const;
export type ScheduledTasksPluginId = (typeof SCHEDULED_TASKS_PLUGIN_ID)[number];
