// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `C4i` / export `Iz`.

/** Navigation payload that returns to plugin browse history. */
export function pluginProductBrowseNav(pluginProductEventContext: unknown): {
  pluginProductEventContext: unknown;
  returnToBrowseHistoryEntry: true;
} {
  return {
    pluginProductEventContext,
    returnToBrowseHistoryEntry: true,
  };
}
