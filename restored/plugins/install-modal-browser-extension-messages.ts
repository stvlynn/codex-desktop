// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// defineMessages cluster (bundle `lgu` / export `wt`).

import { defineMessages } from "react-intl";

export const installModalBrowserExtensionMessages = defineMessages({
  codexChromeDescription: {
    id: "plugins.installModal.browserExtension.description",
    defaultMessage:
      "Install this extension in Chrome to connect ChatGPT to your browser",
    description:
      "Description for the required Chrome browser extension in the plugin finish setup modal",
  },
});
