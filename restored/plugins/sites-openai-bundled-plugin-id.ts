// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Composite plugin id `sites@openai-bundled` (bundle `gUn` / export `A5`).
// Built as `${N_e}@${Wl}` where N_e="sites", Wl=OPENAI_BUNDLED_ID.

export const SITES_OPENAI_BUNDLED_PLUGIN_ID = "sites@openai-bundled";
