// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `Oqo` / export `aD` — empty __esm before SVG icon runtime (xmlns/currentColor).

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureSvgIconRuntimeInit(): void {}
