// Restored from ref/webview/assets/createLucideIcon-BrIFp4GW-DKm8WMeb.js
// Stock lucide-react createLucideIcon — thin npm re-export (not a local body).
// Bundle also exported an esmInit side-effect (`n`); consumers that only need
// the factory should import createLucideIcon from this shim or lucide-react.

export { createLucideIcon } from "lucide-react";
export { createLucideIcon as default } from "lucide-react";
