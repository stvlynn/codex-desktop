// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Artifact / file-tree kind icon (bundle `NHi`).

import type { SVGProps, ReactElement } from "react";
export type HashesFileIconProps = SVGProps<SVGSVGElement>;
export function HashesFileIcon({
  ...props
}: HashesFileIconProps): ReactElement {
  const e = props;
  return (
    <svg
      width={16}
      height={16}
      viewBox={`0 0 16 16`}
      fill={`none`}
      xmlns={`http://www.w3.org/2000/svg`}
      {...e}
    >
      <path
        d={`M2.6665 3.6665H13.3332`}
        stroke={`currentColor`}
        strokeWidth={1.66667}
        strokeLinecap={`round`}
      />
      <path
        d={`M2.6665 12.3335H13.3332`}
        stroke={`currentColor`}
        strokeWidth={1.66667}
        strokeLinecap={`round`}
      />
      <path
        d={`M2.6665 8H7.99984`}
        stroke={`currentColor`}
        strokeWidth={1.66667}
        strokeLinecap={`round`}
      />
    </svg>
  );
}
