// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `hDa` / export `lF`.

export function isTrustedContactExperimentId(id: string): boolean {
  return (
    id === "add_trusted_contact_us4" ||
    id === "add_trusted_contact_us5" ||
    id === "notify_trusted_contact_us5"
  );
}
