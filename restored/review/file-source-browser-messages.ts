// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// defineMessages cluster (bundle `uxo` near export `IA`).

import { defineMessages } from "react-intl";

export const fileSourceBrowserMessages = defineMessages({
  openFileTabTitle: {
    id: "review.fileSource.browser.tabTitle",
    defaultMessage: "Open file",
    description:
      "Title for a workspace file tab before a file has been selected",
  },
});
