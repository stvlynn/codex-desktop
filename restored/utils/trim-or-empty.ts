// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `iQ` — pure helper.

/** Trim a string; nullish becomes empty string. */
export function trimOrEmpty(value: string | null | undefined): string {
  return value?.trim() ?? "";
}
