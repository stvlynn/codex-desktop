// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `Mj` companion for `SZ` / `wZ` / `xZ`.

/** Join two ids with a NUL separator for Map keys. */
export function nullSeparatedMapKey(left: string, right: string): string {
  return `${left}\0${right}`;
}
