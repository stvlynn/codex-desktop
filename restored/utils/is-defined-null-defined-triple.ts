// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `KJs` / export `wg`.

/** True when first and third values are non-null and the middle is null. */
export function isDefinedNullDefinedTriple(
  first: unknown,
  middle: unknown,
  third: unknown,
): boolean {
  return first != null && middle == null && third != null;
}
