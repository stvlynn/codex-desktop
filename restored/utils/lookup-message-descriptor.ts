// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `M2l` / export `Qr`.

/** Index into an i18n message descriptor table. */
export function lookupMessageDescriptor<T>(
  table: Record<string, T>,
  key: string,
): T | undefined {
  return table[key];
}
