// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `VGt` (companion for `PGt` / `Rnt`).

/** Legacy `decodeURIComponent(escape(value))` decode helper. */
export function decodeUriComponentEscape(value: string): string {
  return decodeURIComponent(escape(value));
}
