// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `ou` / export `_mt`.

/** First element of an array, or null. */
export function firstOrNull<T>(
  items: readonly T[] | null | undefined,
): T | null {
  return items?.[0] ?? null;
}
