// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Materialized via extractFn(internal `Ku`) / export `Xft`.
// npm identity: @formatjs/intl error classes (`[@formatjs/intl Error …]`).

export {
  IntlError,
  IntlErrorCode,
  IntlFormatError,
  InvalidConfigError,
  MessageFormatError,
  MissingDataError,
  MissingTranslationError,
  UnsupportedFormatterError,
} from "@formatjs/intl";

export type BindFORMATERROR2Peers = Record<string, unknown>;

/** @deprecated No-op; @formatjs/intl owns the error classes. */
export function setBindFORMATERROR2Peers(_next: BindFORMATERROR2Peers): void {}

/**
 * Bundle export `Xft` / internal `Ku` — FormatJS intl error enum/classes init.
 * No-op under the npm package.
 */
export function bindFORMATERROR2(): void {}
