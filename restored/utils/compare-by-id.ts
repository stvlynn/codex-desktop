// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `gi` — pure helper.

/** Ascending comparator by numeric `id` (bundle subtracts ids). */
export function compareById(a: { id: number }, b: { id: number }): number {
  return a.id - b.id;
}
