// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `R1` — pure helper.

/** Build a `codex:project:` storage/key prefix. */
export function codexProjectKey(projectId: string): string {
  return `codex:project:${projectId}`;
}
