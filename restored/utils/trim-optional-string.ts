// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Trim helper for credit / auto-reload form fields (`iQ` / export `Iy`).

/** Bundle `iQ` / export `Iy`. */
export function trimOptionalString(value: string | null | undefined): string {
  return value?.trim() ?? "";
}
