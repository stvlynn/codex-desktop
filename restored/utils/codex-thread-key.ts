// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `z1` — pure helper.

/** Build a `codex:thread:` storage/key prefix. */
export function codexThreadKey(threadId: string): string {
  return `codex:thread:${threadId}`;
}
