// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `J3o` / export `CT` (co-located with checkout plan URL builder).

/** Return `primary` when truthy; otherwise `fallback`. */
export function coalesceTruthy<T, U>(primary: T, fallback: U): T | U {
  return primary || fallback;
}
