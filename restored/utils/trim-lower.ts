// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `vX` — pure helper.

/** Trim and lowercase a string. */
export function trimLower(value: string): string {
  return value.trim().toLowerCase();
}
