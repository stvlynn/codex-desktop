// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `IEa` / export `gF`.

export function joinColonKeyReversed(left: string, right: string | number): string {
  return `${String(right)}:${left}`;
}
