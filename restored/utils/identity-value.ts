// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `nxe` / export `Hmt`.

/** Identity passthrough used beside cwd helpers. */
export function identityValue<T>(value: T): T {
  return value;
}
