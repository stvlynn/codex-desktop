// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `c$r` — export looked up below if present.

/** True when value has an `input_items` own/enumerable key. */
export function hasInputItemsField(value: object): boolean {
  return "input_items" in value;
}
