// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `sDo` / export `dk`.

/** Read the `isSource` flag from an activity-like object. */
export function isSourceFlag(value: {
  isSource?: boolean;
}): boolean | undefined {
  return value.isSource;
}
