// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `vi` — pure helper.

/** `map.set(key, value)` void wrapper. */
export function mapSet<K, V>(map: Map<K, V>, key: K, value: V): void {
  map.set(key, value);
}
