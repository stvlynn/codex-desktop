// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Materialized via extractFn(internal `Je`) / export `Mvt`.
// npm identity: @tanstack/react-query `QueryClient`.

export {
  QueryClient,
  QueryCache,
  MutationCache,
  QueryObserver,
  InfiniteQueryObserver,
  QueriesObserver,
  MutationObserver,
  notifyManager,
  onlineManager,
  focusManager,
  hashKey,
  keepPreviousData,
} from "@tanstack/react-query";

export type BindMvtBindableHelperPeers = Record<string, unknown>;

/** @deprecated No-op; QueryClient comes from @tanstack/react-query. */
export function setBindMvtBindableHelperPeers(
  _next: BindMvtBindableHelperPeers,
): void {}

/**
 * Bundle export `Mvt` / internal `Je` — QueryClient class init.
 * Returns stock QueryClient under the npm package.
 */
export { QueryClient as bindMvtBindableHelper } from "@tanstack/react-query";
