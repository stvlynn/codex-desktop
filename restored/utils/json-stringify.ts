// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `qg` — pure helper.

/** `JSON.stringify` wrapper. */
export function jsonStringify(value: unknown): string {
  return JSON.stringify(value);
}
