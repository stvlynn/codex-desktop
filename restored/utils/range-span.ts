// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `Xd` — pure helper.

/** `max - min` span of a numeric range. */
export function rangeSpan(range: { min: number; max: number }): number {
  return range.max - range.min;
}
