// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Spacing constants for expanded / collapsed activity stacks (`Bge` / `Vge` / `Hge`).

/** Gap between expanded activity cards (`Bge`). */
export const ACTIVITY_STACK_ITEM_GAP_PX = 8;

/** Extra viewport padding when filtering expanded slots (`Vge`). */
export const ACTIVITY_STACK_VIEWPORT_PAD_PX = 56;

/** Collapsed-stack peel offset per card (`Hge`). */
export const ACTIVITY_STACK_COLLAPSED_PEEL_PX = 4;
