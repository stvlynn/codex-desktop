// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `ygt` — pure helper.

/** True for `'` (39) or `"` (34) char codes. */
export function isQuoteCharCode(code: number): boolean {
  return code === 39 || code === 34;
}
