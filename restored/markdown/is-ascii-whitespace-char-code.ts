// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle export `fG` — pure helper.

/** True for space/tab/LF/CR char codes. */
export function isAsciiWhitespaceCharCode(code: number): boolean {
  return code == 32 || code == 9 || code == 10 || code == 13;
}
