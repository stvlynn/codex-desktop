// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `QKn` / export `n5` — empty __esm before [parseDirectives] logging helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureParseDirectivesLogInit(): void {}
