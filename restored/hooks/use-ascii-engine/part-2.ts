// Restored from ref/webview/assets/use-ascii-engine-DfvLy_CH.js
// Wave GA — full polished body from `use-ascii-engine-DfvLy_CH/auto-polished.tsx` with companion import rewrite.
// Soft-deferred host replaced; NOT app-initial extractFn / promote / producer barrel.
// Residual companion stubs: 1 (verified 6/6).
// AST split 2/3
/* split-lane-import-depth:1 */

import {
  jsxRuntime as getJsxRuntime,
  react,
  reactCompilerRuntime,
} from "../../boundaries/react-cjs-runtime";
import { commonJsInit, esmInit } from "../../runtime/rolldown-runtime";

/** split companion stub */
const harbor: any = undefined;

export function useAsciiEngineN(tee: unknown) {
  let [union, vent] = jade.useState(tee?.initialMode ?? "noise"),
    [wye, zener] = jade.useState("@%#*+=-:. "),
    [alphaPrime, _useAsciiEngineR] = jade.useState(0),
    [bravoPrime, copperPrime] = jade.useState(tee?.initialColumns ?? 50),
    [deltaPrime, echoPrime] = jade.useState(tee?.initialRows ?? 30),
    [_useAsciiEngineI] = jade.useState(20),
    [falconPrime, gammaPrime] = jade.useState(false),
    harborPrime = jade.useMemo(
      () => [
        "█▓▒░ ",
        "■□▲△●○◆◇",
        "⎺⎻⎼⎽⎾⎿",
        "o p e n a i ",
        "█▉▊▋▌▍▎▏",
        "█▓▒░-=:. ",
        "█▇▆▅▄▃▂▁",
        "C O D E X",
        "█■▲●◉○. ",
        "WMBRXVIl. ",
        "█#A*-. ",
        "●◉○· ",
      ],
      [],
    );
  jade.useEffect(() => {
    typeof tee?.initialColumns == "number" && copperPrime(tee.initialColumns);
  }, [tee?.initialColumns]);
  jade.useEffect(() => {
    typeof tee?.initialRows == "number" && echoPrime(tee.initialRows);
  }, [tee?.initialRows]);
  let indigoPrime = jade.useRef(null),
    jadePrime = jade.useRef(null),
    _useAsciiEngineN = jade.useRef(null),
    [_useAsciiEngineT, kitePrime] = jade.useState([]),
    lemonPrime = jade.useRef(0),
    marblePrime = jade.useRef(tee?.preferredVideoKeyword),
    nickelPrime = jade.useMemo(() => indigo.makeNoise3D(Date.now()), []),
    onyxPrime = jade.useRef(0),
    pearlPrime = jade.useRef(null),
    quartzPrime = jade.useRef(0),
    riverPrime = jade.useRef(""),
    slatePrime = jade.useRef(null),
    timberPrime = jade.useCallback(
      (hazelPrime, ivoryPrime, jasperPrime) => {
        let kelpPrime = [];
        for (let lotusPrime = 0; lotusPrime < ivoryPrime; lotusPrime++) {
          let mintPrime = [];
          for (let novaPrime = 0; novaPrime < hazelPrime; novaPrime++) {
            let olivePrime =
              ((nickelPrime(novaPrime * 0.15, lotusPrime * 0.15, jasperPrime) +
                1) /
                2) *
              255;
            mintPrime.push(Math.round(olivePrime));
          }
          kelpPrime.push(mintPrime);
        }
        return kelpPrime;
      },
      [nickelPrime],
    ),
    umbraPrime = jade.useCallback((prismPrime, quillPrime) => {
      let reefPrime = prismPrime.length,
        sagePrime = reefPrime > 0 ? prismPrime[0].length : 0,
        topazPrime = prismPrime.map((item) => item.slice());
      for (let ultraPrime = 0; ultraPrime < reefPrime; ultraPrime++)
        for (let vaporPrime = 0; vaporPrime < sagePrime; vaporPrime++) {
          let __useAsciiEngineR = topazPrime[ultraPrime][vaporPrime],
            wheatPrime =
              Math.round((__useAsciiEngineR / 255) * (quillPrime - 1)) *
              (255 / (quillPrime - 1));
          topazPrime[ultraPrime][vaporPrime] = wheatPrime;
          let yarnPrime = __useAsciiEngineR - wheatPrime;
          vaporPrime + 1 < sagePrime &&
            (topazPrime[ultraPrime][vaporPrime + 1] += (yarnPrime * 7) / 16);
          ultraPrime + 1 < reefPrime &&
            vaporPrime > 0 &&
            (topazPrime[ultraPrime + 1][vaporPrime - 1] +=
              (yarnPrime * 3) / 16);
          ultraPrime + 1 < reefPrime &&
            (topazPrime[ultraPrime + 1][vaporPrime] += (yarnPrime * 5) / 16);
          ultraPrime + 1 < reefPrime &&
            vaporPrime + 1 < sagePrime &&
            (topazPrime[ultraPrime + 1][vaporPrime + 1] += yarnPrime / 16);
        }
      for (let zephyrPrime = 0; zephyrPrime < reefPrime; zephyrPrime++)
        for (let acornPrime = 0; acornPrime < sagePrime; acornPrime++)
          topazPrime[zephyrPrime][acornPrime] = Math.max(
            0,
            Math.min(255, topazPrime[zephyrPrime][acornPrime]),
          );
      return topazPrime;
    }, []),
    violetPrime = jade.useCallback(() => {
      let bloomPrime = indigoPrime.current,
        coralPrime = jadePrime.current,
        driftPrime = _useAsciiEngineN.current;
      if (!bloomPrime || !coralPrime || !driftPrime) return;
      let eaglePrime = bloomPrime.videoWidth,
        frostPrime = bloomPrime.videoHeight;
      if (
        !Number.isFinite(eaglePrime) ||
        !Number.isFinite(frostPrime) ||
        eaglePrime < 2 ||
        frostPrime < 2
      )
        return;
      let glidePrime = bravoPrime,
        __useAsciiEngineR = deltaPrime,
        honeyPrime = glidePrime / Math.max(1, 2 * __useAsciiEngineR),
        irisPrime = eaglePrime / frostPrime,
        __useAsciiEngineI,
        jewelPrime,
        knollPrime,
        lunarPrime;
      irisPrime > honeyPrime
        ? ((lunarPrime = frostPrime),
          (knollPrime = Math.max(1, frostPrime * honeyPrime)),
          (__useAsciiEngineI = Math.max(0, (eaglePrime - knollPrime) / 2)),
          (jewelPrime = 0))
        : ((knollPrime = eaglePrime),
          (lunarPrime = Math.max(1, eaglePrime / honeyPrime)),
          (__useAsciiEngineI = 0),
          (jewelPrime = Math.max(0, (frostPrime - lunarPrime) / 2)));
      try {
        driftPrime.drawImage(
          bloomPrime,
          __useAsciiEngineI,
          jewelPrime,
          knollPrime,
          lunarPrime,
          0,
          0,
          glidePrime,
          __useAsciiEngineR,
        );
      } catch {}
    }, [bravoPrime, deltaPrime]),
    willowPrime = jade.useCallback(() => {
      let mossPrime = jadePrime.current,
        northPrime = _useAsciiEngineN.current;
      if (!mossPrime || !northPrime) return [];
      let orbitPrime = northPrime.getImageData(
          0,
          0,
          bravoPrime,
          deltaPrime,
        ).data,
        pinePrime = [];
      for (let questPrime = 0; questPrime < deltaPrime; questPrime++) {
        let ridgePrime = [];
        for (let stormPrime = 0; stormPrime < bravoPrime; stormPrime++) {
          let tidePrime = (questPrime * bravoPrime + stormPrime) * 4,
            unityPrime = orbitPrime[tidePrime],
            __useAsciiEngineR = orbitPrime[tidePrime + 1],
            valePrime = orbitPrime[tidePrime + 2],
            wavePrime =
              0.299 * unityPrime +
              0.587 * __useAsciiEngineR +
              0.114 * valePrime;
          ridgePrime.push(Math.round(wavePrime));
        }
        pinePrime.push(ridgePrime);
      }
      return pinePrime;
    }, [bravoPrime, deltaPrime]),
    xenonPrime = jade.useCallback(
      (apexPrime) => {
        let brookPrime = jadePrime.current,
          cliffPrime = _useAsciiEngineN.current;
        if (!brookPrime || !cliffPrime) return [];
        let duskPrime = cliffPrime.getImageData(
            0,
            0,
            bravoPrime,
            deltaPrime,
          ).data,
          elmPrime = [];
        for (let grovePrime = 0; grovePrime < deltaPrime; grovePrime++) {
          let hillPrime = [];
          for (let islePrime = 0; islePrime < bravoPrime; islePrime++) {
            let juniperPrime = (grovePrime * bravoPrime + islePrime) * 4,
              lagoonPrime = duskPrime[juniperPrime],
              ___useAsciiEngineR = duskPrime[juniperPrime + 1],
              meadowPrime = duskPrime[juniperPrime + 2],
              nestPrime =
                0.299 * lagoonPrime +
                0.587 * ___useAsciiEngineR +
                0.114 * meadowPrime;
            hillPrime.push(Math.round(nestPrime));
          }
          elmPrime.push(hillPrime);
        }
        let __useAsciiEngineR = umbraPrime(elmPrime, wye.length),
          fernPrime = [];
        for (let oakPrime = 0; oakPrime < deltaPrime; oakPrime++) {
          let petalPrime = "";
          for (let quietPrime = 0; quietPrime < bravoPrime; quietPrime++) {
            let rainPrime = Math.round(
              apexPrime
                ? (__useAsciiEngineR[oakPrime][quietPrime] / 255) *
                    (wye.length - 1)
                : (1 - __useAsciiEngineR[oakPrime][quietPrime] / 255) *
                    (wye.length - 1),
            );
            Number.isNaN(rainPrime) && (rainPrime = 0);
            rainPrime = Math.max(0, Math.min(wye.length - 1, rainPrime));
            petalPrime += wye[rainPrime];
          }
          fernPrime.push(petalPrime);
        }
        return fernPrime;
      },
      [wye, bravoPrime, deltaPrime, umbraPrime],
    ),
    yellowPrime = jade.useCallback(() => {
      let seedPrime = indigoPrime.current;
      if (!seedPrime) return false;
      let trailPrime =
          Number.isFinite(seedPrime.videoWidth) &&
          Number.isFinite(seedPrime.videoHeight) &&
          seedPrime.videoWidth > 1 &&
          seedPrime.videoHeight > 1,
        urnPrime = (seedPrime.readyState ?? 0) >= 2;
      return trailPrime && urnPrime && !seedPrime.paused;
    }, []),
    zincPrime = jade.useCallback(() => {
      jadePrime.current ||
        ((jadePrime.current = document.createElement("canvas")),
        (jadePrime.current.style.display = "none"),
        document.body.appendChild(jadePrime.current));
      _useAsciiEngineN.current ||= jadePrime.current.getContext("2d", {
        willReadFrequently: true,
      });
      jadePrime.current &&
        ((jadePrime.current.width = bravoPrime),
        (jadePrime.current.height = deltaPrime));
      let vinePrime = [];
      if (union === "video") {
        if (yellowPrime()) {
          violetPrime();
          vinePrime = xenonPrime(true);
        } else {
          let windPrime = timberPrime(
              bravoPrime,
              deltaPrime,
              onyxPrime.current,
            ),
            yarrowPrime = umbraPrime(windPrime, wye.length);
          for (let azurePrime = 0; azurePrime < deltaPrime; azurePrime++) {
            let birchPrime = "";
            for (let canyonPrime = 0; canyonPrime < bravoPrime; canyonPrime++) {
              let dewPrime = Math.round(
                (1 - yarrowPrime[azurePrime][canyonPrime] / 255) *
                  (wye.length - 1),
              );
              Number.isNaN(dewPrime) && (dewPrime = 0);
              dewPrime = Math.max(0, Math.min(wye.length - 1, dewPrime));
              birchPrime += wye[dewPrime];
            }
            vinePrime.push(birchPrime);
          }
        }
      } else if (union === "composite") {
        let everPrime = timberPrime(bravoPrime, deltaPrime, onyxPrime.current),
          fieldPrime = umbraPrime(everPrime, wye.length),
          grainPrime = [];
        for (let havenPrime = 0; havenPrime < deltaPrime; havenPrime++) {
          let inkPrime = "";
          for (
            let jadeitePrime = 0;
            jadeitePrime < bravoPrime;
            jadeitePrime++
          ) {
            let kernelPrime = Math.round(
              (1 - fieldPrime[havenPrime][jadeitePrime] / 255) *
                (wye.length - 1),
            );
            Number.isNaN(kernelPrime) && (kernelPrime = 0);
            kernelPrime = Math.max(0, Math.min(wye.length - 1, kernelPrime));
            inkPrime += wye[kernelPrime];
          }
          grainPrime.push(inkPrime);
        }
        if (yellowPrime()) {
          violetPrime();
          let leafPrime = xenonPrime(true),
            maplePrime = willowPrime(),
            nimbusPrime = [];
          for (let opalPrime = 0; opalPrime < deltaPrime; opalPrime++) {
            let plumePrime = "";
            for (
              let quillowPrime = 0;
              quillowPrime < bravoPrime;
              quillowPrime++
            ) {
              let __useAsciiEngineR =
                (maplePrime[opalPrime]?.[quillowPrime] ?? 0) > 110;
              plumePrime += __useAsciiEngineR
                ? leafPrime[opalPrime][quillowPrime]
                : grainPrime[opalPrime][quillowPrime];
            }
            nimbusPrime.push(plumePrime);
          }
          vinePrime = nimbusPrime;
        } else vinePrime = grainPrime;
      } else {
        let rootPrime = timberPrime(bravoPrime, deltaPrime, onyxPrime.current),
          silkPrime = umbraPrime(rootPrime, wye.length);
        for (let thornPrime = 0; thornPrime < deltaPrime; thornPrime++) {
          let uplandPrime = "";
          for (let vistaPrime = 0; vistaPrime < bravoPrime; vistaPrime++) {
            let wispPrime = Math.round(
              (1 - silkPrime[thornPrime][vistaPrime] / 255) * (wye.length - 1),
            );
            Number.isNaN(wispPrime) && (wispPrime = 0);
            wispPrime = Math.max(0, Math.min(wye.length - 1, wispPrime));
            uplandPrime += wye[wispPrime];
          }
          vinePrime.push(uplandPrime);
        }
      }
      return (
        (!vinePrime.length || vinePrime.every((item) => !item.trim())) &&
          (vinePrime = Array.from(
            {
              length: deltaPrime,
            },
            () => "@".repeat(bravoPrime),
          )),
        (onyxPrime.current += 0.03),
        vinePrime
      );
    }, [
      wye,
      bravoPrime,
      deltaPrime,
      violetPrime,
      xenonPrime,
      timberPrime,
      umbraPrime,
      union,
      yellowPrime,
      willowPrime,
    ]),
    amberPrime = jade.useCallback(() => {
      if (!_useAsciiEngineT.length) return "";
      let yonderPrime = lemonPrime.current % _useAsciiEngineT.length;
      return (
        (lemonPrime.current =
          (lemonPrime.current + 1) % Math.max(1, _useAsciiEngineT.length)),
        _useAsciiEngineT[yonderPrime]
      );
    }, [_useAsciiEngineT]),
    basaltPrime = jade.useCallback(
      async (zenithPrime = false) => {
        let anvilPrime = indigoPrime.current;
        anvilPrime ??
          ((anvilPrime = document.createElement("video")),
          (anvilPrime.style.display = "none"),
          (anvilPrime.loop = true),
          (anvilPrime.muted = true),
          anvilPrime.setAttribute("playsinline", ""),
          document.body.appendChild(anvilPrime),
          (indigoPrime.current = anvilPrime));
        anvilPrime.muted = true;
        anvilPrime.setAttribute("playsinline", "");
        let beaconPrime = () => {
          if (!marblePrime.current) return "";
          let cragPrime = marblePrime.current.toLowerCase();
          return (
            _useAsciiEngineT.find((item) =>
              item.toLowerCase().includes(cragPrime),
            ) || ""
          );
        };
        if (zenithPrime) {
          let domePrime = beaconPrime() || amberPrime();
          if (domePrime) {
            try {
              let eddyPrime = await (await fetch(domePrime)).blob(),
                fjordPrime = URL.createObjectURL(eddyPrime);
              if (slatePrime.current)
                try {
                  URL.revokeObjectURL(slatePrime.current);
                } catch {}
              slatePrime.current = fjordPrime;
              anvilPrime.src = fjordPrime;
            } catch {}
            anvilPrime.loop = true;
          }
        } else if (!anvilPrime.src) {
          let glenPrime = beaconPrime() || amberPrime();
          if (glenPrime) {
            try {
              let hearthPrime = await (await fetch(glenPrime)).blob(),
                inletPrime = URL.createObjectURL(hearthPrime);
              if (slatePrime.current)
                try {
                  URL.revokeObjectURL(slatePrime.current);
                } catch {}
              slatePrime.current = inletPrime;
              anvilPrime.src = inletPrime;
            } catch {}
            anvilPrime.loop = true;
          }
        }
        try {
          (anvilPrime.readyState ?? 0) < 2 &&
            (await new Promise((jettyPrime) => {
              let knobPrime = () => {
                anvilPrime.removeEventListener("loadeddata", knobPrime);
                jettyPrime();
              };
              anvilPrime.addEventListener("loadeddata", knobPrime, {
                once: true,
              });
            }));
          anvilPrime.paused && (await anvilPrime.play());
        } catch {}
        jadePrime.current ||
          ((jadePrime.current = document.createElement("canvas")),
          (jadePrime.current.style.display = "none"),
          document.body.appendChild(jadePrime.current));
        !_useAsciiEngineN.current &&
          jadePrime.current &&
          (_useAsciiEngineN.current = jadePrime.current.getContext("2d", {
            willReadFrequently: true,
          }));
      },
      [amberPrime, _useAsciiEngineT],
    ),
    cedarPrime = jade.useCallback(() => {
      if (!indigoPrime.current) {
        let ledgePrime = document.createElement("video");
        ledgePrime.style.display = "none";
        ledgePrime.loop = true;
        ledgePrime.muted = true;
        ledgePrime.setAttribute("playsinline", "");
        document.body.appendChild(ledgePrime);
        indigoPrime.current = ledgePrime;
      }
      jadePrime.current ||
        ((jadePrime.current = document.createElement("canvas")),
        (jadePrime.current.style.display = "none"),
        document.body.appendChild(jadePrime.current));
      !_useAsciiEngineN.current &&
        jadePrime.current &&
        (_useAsciiEngineN.current = jadePrime.current.getContext("2d", {
          willReadFrequently: true,
        }));
    }, []),
    daisyPrime = jade.useCallback(async () => {
      cedarPrime();
      let mirePrime = indigoPrime.current;
      if (!mirePrime || !_useAsciiEngineT.length) return;
      let nookPrime = mirePrime.src || "",
        oxbowPrime = _useAsciiEngineT.findIndex(
          (item) =>
            nookPrime.endsWith(item) ||
            nookPrime === item ||
            nookPrime.includes(item),
        );
      oxbowPrime < 0 &&
        (oxbowPrime =
          lemonPrime.current % Math.max(1, _useAsciiEngineT.length));
      let pondPrime = (oxbowPrime + 1) % _useAsciiEngineT.length;
      lemonPrime.current = pondPrime;
      let quarryPrime = _useAsciiEngineT[pondPrime];
      if (quarryPrime)
        try {
          let rapidsPrime = await (await fetch(quarryPrime)).blob(),
            spurPrime = URL.createObjectURL(rapidsPrime);
          if (slatePrime.current)
            try {
              URL.revokeObjectURL(slatePrime.current);
            } catch {}
          slatePrime.current = spurPrime;
          mirePrime.src = spurPrime;
        } catch {}
      mirePrime.loop = true;
      mirePrime.muted = true;
      try {
        await mirePrime.play();
      } catch {}
    }, [cedarPrime, _useAsciiEngineT]);
  jade.useEffect(() => {
    (async () => {
      try {
        let torPrime = Object.values(Object.assign({}));
        kitePrime(torPrime);
      } catch {}
    })();
  }, []);
  jade.useEffect(() => {
    let updraftPrime = (event) => {
      if (
        event.metaKey &&
        (event.key === "/" || event.key === "?" || event.code === "Slash")
      ) {
        if ((event.preventDefault(), event.repeat)) return;
        let vergePrime = union === "video" ? "noise" : "video";
        vent(vergePrime);
        vergePrime === "video" &&
          (basaltPrime(true), (quartzPrime.current = 0));
        return;
      }
      if (event.metaKey && event.key === ".") {
        if ((event.preventDefault(), event.repeat)) return;
        let weirPrime = (alphaPrime + 1) % harborPrime.length;
        _useAsciiEngineR(weirPrime);
        zener(harborPrime[weirPrime]);
        return;
      }
      if (
        (event.metaKey && event.key.toLowerCase() === "m") ||
        (event.metaKey && event.shiftKey && event.key.toLowerCase() === "m") ||
        (event.altKey && event.key.toLowerCase() === "m")
      ) {
        if ((event.preventDefault(), event.repeat)) return;
        union === "video" ? daisyPrime() : (vent("video"), basaltPrime(true));
        return;
      }
    };
    return (
      window.addEventListener("keydown", updraftPrime),
      () => {
        window.removeEventListener("keydown", updraftPrime);
      }
    );
  }, [alphaPrime, harborPrime, daisyPrime, basaltPrime, union, vent]);
  jade.useEffect(() => {
    let yardPrime = 1e3 / _useAsciiEngineI,
      anchorPrime = (cogPrime) => {
        try {
          if (document.hidden) return;
          if (cogPrime - quartzPrime.current >= yardPrime - 1) {
            let discPrime = zincPrime(),
              edgePrime = discPrime.join("\n");
            edgePrime !== riverPrime.current &&
              ((riverPrime.current = edgePrime), garnetPrime(discPrime));
            quartzPrime.current = cogPrime;
          }
        } catch {
        } finally {
          pearlPrime.current = window.requestAnimationFrame(anchorPrime);
        }
      };
    pearlPrime.current = window.requestAnimationFrame(anchorPrime);
    let boltPrime = () => {
      document.hidden || (quartzPrime.current = 0);
    };
    return (
      document.addEventListener("visibilitychange", boltPrime),
      () => {
        pearlPrime.current != null &&
          (window.cancelAnimationFrame(pearlPrime.current),
          (pearlPrime.current = null));
        document.removeEventListener("visibilitychange", boltPrime);
      }
    );
  }, [zincPrime, _useAsciiEngineI]);
  jade.useEffect(
    () => () => {
      try {
        let forgePrime = indigoPrime.current;
        if (forgePrime) {
          try {
            forgePrime.paused || forgePrime.pause();
          } catch {}
          forgePrime.srcObject = null;
          try {
            forgePrime.remove();
          } catch {}
        }
      } catch {}
      if (jadePrime.current)
        try {
          jadePrime.current.remove();
        } catch {}
      if (slatePrime.current) {
        try {
          URL.revokeObjectURL(slatePrime.current);
        } catch {}
        slatePrime.current = null;
      }
      indigoPrime.current = null;
      jadePrime.current = null;
      _useAsciiEngineN.current = null;
    },
    [],
  );
  let emberPrime = jade.useRef("noise"),
    [flintPrime, garnetPrime] = jade.useState([]);
  return (
    jade.useEffect(() => {
      let gearPrime = emberPrime.current;
      (union === "video" || union === "composite") &&
        basaltPrime(gearPrime !== union);
      emberPrime.current = union;
    }, [union, basaltPrime]),
    jade.useEffect(() => {
      (union === "video" || union === "composite") &&
        _useAsciiEngineT.length &&
        basaltPrime(true);
    }, [_useAsciiEngineT, union, basaltPrime]),
    {
      mode: union,
      setMode: vent,
      asciiChars: wye,
      setAsciiChars: zener,
      columns: bravoPrime,
      setColumns: copperPrime,
      rows: deltaPrime,
      setRows: echoPrime,
      lines: flintPrime,
      showControls: falconPrime,
      setShowControls: gammaPrime,
    }
  );
}
var indigo, jade;
export const useAsciiEngineT = esmInit(() => {
  indigo = harbor();
  jade = commonJsInit(react(), 1);
});
