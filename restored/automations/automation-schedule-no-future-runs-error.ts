// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Error copy when an automation schedule has no upcoming occurrence (`Mme`).

/** Bundle `Mme` (co-located with `Hme` / packet-004 automation schema init). */
export const AUTOMATION_SCHEDULE_NO_FUTURE_RUNS_ERROR =
  "Automation schedule has no future runs. Choose a schedule with an occurrence after the current time.";
