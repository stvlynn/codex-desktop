// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Local-thread route base + param pattern (bundle `hTe`/`gTe` / export `Jpt`).

export const LOCAL_CONVERSATION_ROUTE_BASE = "/local";

/** React-router style path (bundle `gTe` / export `Jpt`). */
export const LOCAL_CONVERSATION_ROUTE = `${LOCAL_CONVERSATION_ROUTE_BASE}/:conversationId`;
