// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `_iu` / export `Wn`.

/** TanStack query key tuple for primary-runtime diagnostics. */
export function primaryRuntimeDiagnosticsQueryKey(
  hostId: string,
): readonly ["app-host", "primary-runtime", "diagnostics", string] {
  return ["app-host", "primary-runtime", "diagnostics", hostId];
}
