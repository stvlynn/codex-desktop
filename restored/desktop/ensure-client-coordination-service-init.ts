// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `pp` / export `Kut` — empty __esm before client coordination / thread queued follow-ups.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureClientCoordinationServiceInit(): void {}
