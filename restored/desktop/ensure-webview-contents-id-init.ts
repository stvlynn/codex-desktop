// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `SR` / export `hV` — empty __esm before webview getWebContentsId helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureWebviewContentsIdInit(): void {}
