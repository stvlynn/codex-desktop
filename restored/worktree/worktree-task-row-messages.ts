// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Title / confirm copy used by the worktree sidebar task row (`YVl`).

import { defineMessages } from "react-intl";

/**
 * Inline FormattedMessage descriptors from `YVl` (export alias `ho`).
 * The full row component stays deferred (depends on the shared task-row shell).
 */
export const worktreeTaskRowMessages = defineMessages({
  worktreeInitFailedTitle: {
    id: "recentTasks.worktreeInitFailedTitle",
    defaultMessage: "Worktree init failed",
    description: "Worktree row title when the init script fails",
  },
  confirmArchiveTask: {
    id: "codex.localTaskRow.confirmArchiveTask",
    defaultMessage: "Confirm",
    description: "Confirmation button for archiving a local task",
  },
});
