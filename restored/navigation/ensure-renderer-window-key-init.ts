// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `ige` / export `wgt` — empty __esm before renderer window key builder (browser comment popup).

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureRendererWindowKeyInit(): void {}
