// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Empty / thin ESM init thunks that register-app-actions calls for module
// ordering (bundle `hni` / export `BW`).

/**
 * Bundle `hni` / export `BW` — empty `__esm` init (`e(() => {})`).
 * Consumers call it before defining appearance / window action schemas;
 * there is no runtime body to restore.
 */
export function ensureAppActionRuntimeInit(): void {}
