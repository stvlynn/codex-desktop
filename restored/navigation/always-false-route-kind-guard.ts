// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `l1o` / `u1o` / exports `yE` / `fE` (comma-operator always-false).

/** Historical always-false route guard retained for export parity. */
export function alwaysFalseRouteKindGuard(
  _route?: { value?: { routeKind?: unknown } } | null,
  _other?: unknown,
  _flag: boolean = true,
): false {
  return false;
}
