// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `ER` / export `sV`; companion `uJi` = `codex_apps`.

const CODEX_APPS_SURFACE_ID = "codex_apps";

/** True when id is the Codex Apps surface. */
export function isCodexAppsSurfaceId(id: string): boolean {
  return id === CODEX_APPS_SURFACE_ID;
}
