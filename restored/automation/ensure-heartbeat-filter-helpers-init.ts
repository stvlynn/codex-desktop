// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `rg` / export `rct` — empty __esm before compact null filter used by heartbeat parsers.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureHeartbeatFilterHelpersInit(): void {}
