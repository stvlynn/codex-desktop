// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Wave lane-B — soft host consolidated to owner `utils/mrt-co-shell.ts`
// (export `mrt` / internal `bUt` already filled; do not invent body). via=filled-owner

export {
  bindMrtCoShell as mrt,
  setBindMrtCoShellPeers,
  setBindMrtCoShellPeers as bindMrt,
  type BindMrtCoShellPeers,
} from "../utils/mrt-co-shell";
