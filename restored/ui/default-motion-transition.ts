// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Default framer/motion-style transition (bundle `Mk` / export `S1`).
// duration is 300ms expressed in seconds; ease differs from EASE_OUT_EXPO.

export const DEFAULT_MOTION_TRANSITION = {
  duration: 300 / 1000,
  ease: [0.19, 1, 0.22, 1] as readonly [number, number, number, number],
};
