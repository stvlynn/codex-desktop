// Restored from ref/webview/assets/unknown-chunk-for-split-width-fraction.js
// Wave HH — false invent eliminated (via=false-lift). Soft bindable
// splitWidthFraction host removed: unknown-chunk never existed; app-initial
// exportSources / auto-polished / assets have no `splitWidthFraction` symbol;
// no public importers. Nearby `$Hr`→`widthRatio` / `rightPanelWidthRatio` is NOT
// a proven alias — do not invent extractFn or consolidate.
// Terminal: tombstone only — no public splitWidthFraction runtime API.

export {};
