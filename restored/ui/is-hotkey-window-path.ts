// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Wave DU companion — extractFn(internal `QH`) used by ChromeBackgroundPanel.

/** Bundle internal `QH` — true on the hotkey-window route. */
export function isHotkeyWindowPath(pathname: string): boolean {
  return pathname === "/hotkey-window";
}
