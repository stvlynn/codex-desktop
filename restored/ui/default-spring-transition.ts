// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Default spring motion config (bundle `WD` / export `w0`).

export const DEFAULT_SPRING_TRANSITION = {
  type: "spring" as const,
  duration: 0.5,
  bounce: 0.1,
};
