// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Public re-export surface for DropdownMenuPopover (`KB` / `VR`).

export { DropdownMenuPopover } from "./popover";
export type { DropdownMenuPopoverProps } from "./types";

/** Bundle path ESM init retained as no-op (`Qlt` / `qS`). */
export function ensureDropdownMenuPopoverInit(): void {}

/** Legacy no-op — peers are now inlined. */
export function setDropdownMenuPopoverPeers(_next: unknown): void {}
