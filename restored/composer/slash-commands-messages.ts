// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Slash command palette empty state (`G7o` / export `Xw` init thunk).

import { defineMessages } from "react-intl";

export const slashCommandsMessages = defineMessages({
  noResults: {
    id: "composer.slashCommands.noResults",
    defaultMessage: "No commands",
    description: "Shown when no slash commands match",
  },
});
