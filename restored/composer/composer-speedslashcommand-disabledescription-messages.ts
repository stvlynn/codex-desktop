// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// defineMessages cluster (bundle `AGs`).

import { defineMessages } from "react-intl";

export const composerSpeedslashcommandDisabledescriptionMessages =
  defineMessages({
    disableDescription: {
      id: "composer.speedSlashCommand.disableDescription",
      defaultMessage: "Turn off {serviceTier} and return to standard speed",
      description:
        "Description for a service tier slash command when that tier is on",
    },
  });
