// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Inline mention chip class names (bundle `Qna` / export `XL`; set in init `cra`).

/** Tailwind group class for inline mention chips (`Qna` / `XL`). */
export const INLINE_MENTION_GROUP_CLASS = "group/inline-mention cursor-pointer";
