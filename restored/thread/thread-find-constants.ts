// Restored from ref/webview/assets/app-initial-C-fROkKo.js
/** Bundle `FBi` — mark class for in-thread find highlights. */
export const CODEX_THREAD_FIND_MATCH_CLASS = "codex-thread-find-match";

/** Bundle `IBi` / export `XH` — active match class (+ event name). */
export const CODEX_THREAD_FIND_ACTIVE_CLASS = "codex-thread-find-active";

/** Bundle `LBi` — data attribute stamped on each highlighted mark. */
export const CONTENT_SEARCH_MATCH_ID_ATTR = "data-content-search-match-id";

/** Bundle `RBi` — style element id injected into shadow roots. */
export const CODEX_THREAD_FIND_SHADOW_STYLE_ID =
  "codex-thread-find-shadow-style";

/** Bundle `WBi` / export `YH` init — snippet context padding (chars). */
export const THREAD_FIND_SNIPPET_PADDING = 24;
