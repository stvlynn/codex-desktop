// Restored from ref/webview/assets/esm-BkXFCz6T.js
//
// @segment/analytics-core re-export boundary (generated by scripts/make-facade.ts
// --reexport, then augmented for inlined @segment/analytics-generic-utils /
// @lukeed/uuid + rolldown ESM init no-ops). This bundled chunk IS the third-party
// package "@segment/analytics-core" (with deps inlined by the bundler); consumers
// import it through this thin shim. AI-only imports were tslib helpers
// (__extends / __awaiter / __generator / __rest) — satisfied by the npm package.
// Not a Codex fork.

export {
  isString as A,
  pTimeout as C,
  isFunction as D,
  isNumber as O,
  ContextCancelation as c,
  CoreStats as d,
  PriorityQueue as h,
  CoreEventQueue as i,
  isPlainObject as k,
  CoreContext as l,
  dispatch as n,
  attempt as o,
  CoreEventFactory as w,
  // Semantic names for Stage 3 consumers.
  isString,
  pTimeout,
  isFunction,
  isNumber,
  ContextCancelation,
  CoreStats,
  PriorityQueue,
  CoreEventQueue,
  isPlainObject,
  CoreContext,
  dispatch,
  attempt,
  CoreEventFactory,
  CoreLogger,
  NullStats,
  ON_REMOVE_FROM_FUTURE,
  ValidationError,
  ensure,
  backoff,
  getDelay,
  invokeCallback,
  sleep,
  validateEvent,
} from "@segment/analytics-core";

export {
  createDeferred as b,
  Emitter as v,
  createDeferred,
  Emitter,
} from "@segment/analytics-generic-utils";

export { v4 as m } from "@lukeed/uuid";

/** Bundler ESM init no-op retained for export token `t` (master init). */
export function t(): void {}

/** Bundler ESM init no-op retained for export token `_`. */
export function _(): void {}

/** Bundler ESM init no-op retained for export token `p` (uuid table). */
export function p(): void {}

/** Bundler ESM init no-op retained for export token `E`. */
export function E(): void {}

/** Bundler ESM init no-op retained for export token `S`. */
export function S(): void {}

/** Bundler ESM init no-op retained for export token `T` (EventFactory module). */
export function T(): void {}

/** Bundler ESM init no-op retained for export token `a` (EventQueue module). */
export function a(): void {}

/** Bundler ESM init no-op retained for export token `f` (Stats module). */
export function f(): void {}

/** Bundler ESM init no-op retained for export token `g` (PriorityQueue module). */
export function g(): void {}

/** Bundler ESM init no-op retained for export token `r` (getDelay module). */
export function r(): void {}

/** Bundler ESM init no-op retained for export token `s`. */
export function s(): void {}

/** Bundler ESM init no-op retained for export token `u` (Context module). */
export function u(): void {}

/** Bundler ESM init no-op retained for export token `x` (createDeferred module). */
export function x(): void {}

/** Bundler ESM init no-op retained for export token `y` (Emitter module). */
export function y(): void {}
