// Restored from ref/webview/assets/app-initial-C-fROkKo.js (inlined marked)
// npm shim: marked.
// Bundle bindings: ry exported as frt — marked() with .lexer/.parser attached; (Frt is PopoverPrimitiveRoot / SBt, not marked)
// tGt exported as drt/Drt — rolldown lazy init for marked internals (unnecessary with npm).

export { marked } from "marked";

/** Bundle `oy` / export `prt` — marked.Lexer. */
export { Lexer } from "marked";
