// Restored from ref/webview/assets/app-initial-C-fROkKo.js (inlined uuid)
// npm shim: uuid.
// Bundle bindings (mindmap): uy/lrt≈v4; dy/srt≈rolldown lazy init for uuid
// internals (unnecessary with npm — retained as no-op for call-site shape).

export { v4 } from "uuid";

/** Bundler ESM init no-op retained for export token `srt` / `dy`. */
export function ensureUuidRuntime(): void {}
