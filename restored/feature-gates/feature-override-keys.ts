// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Dynamic-config feature override keys (`sun` / `cun` / `uun`).

/** Feature override key for concurrent reasoning summaries (`sun` / export `O7`). */
export const FEATURE_OVERRIDE_CONCURRENT_REASONING_SUMMARIES_KEY =
  "concurrent_reasoning_summaries";

/** Feature override key for default-mode user-input prompts (`cun` / export `k7`). */
export const FEATURE_OVERRIDE_DEFAULT_MODE_REQUEST_USER_INPUT_KEY =
  "default_mode_request_user_input";

/** Feature override key for thread-scoped tools (`uun` / export `A7`). */
export const FEATURE_OVERRIDE_THREAD_TOOLS_KEY = "thread_tools";
