// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Materialized via extractFn(internal `lXt`) / export `hnt`.
// npm identity: pdfjs-dist `DrawLayer`.

export { DrawLayer, DrawLayer as browser3 } from "../vendor/pdfjs";

export type Browser3Peers = Record<string, unknown>;

/** @deprecated No-op; DrawLayer comes from pdfjs-dist. */
export function setBrowser3Peers(_next: Browser3Peers): void {}
