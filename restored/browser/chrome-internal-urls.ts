// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Chrome internal pages opened from the embedded browser settings UI.

/** Export `cht` / bundle `hve`. */
export const CHROME_DOWNLOADS_URL = "chrome://downloads/";

/** Export `dht` / bundle `_ve`. */
export const CHROME_EXTENSIONS_URL = "chrome://extensions/";

/** Export `mht` / bundle `xve`. */
export const CHROME_HISTORY_URL = "chrome://history/";

/** Export `ght` / bundle `Ove`. */
export const CHROME_PASSWORD_MANAGER_URL = "chrome://password-manager/";

/** Export `vht` / bundle `Dve`. */
export const CHROME_PASSWORD_MANAGER_PASSWORDS_URL =
  "chrome://password-manager/passwords";

/** Export `aht` / bundle `Cve`. */
export const CHROME_SETTINGS_ADDRESSES_URL = "chrome://settings/addresses";

/** Export `Cht` / bundle `jve`. */
export const CHROME_SETTINGS_CONTENT_URL = "chrome://settings/content";

/** Export `Tht` / bundle `Pve`. */
export const CHROME_SETTINGS_COOKIES_URL = "chrome://settings/cookies";

/** Export `bht` / bundle `Nve`. */
export const CHROME_SETTINGS_HANDLERS_URL = "chrome://settings/handlers";
