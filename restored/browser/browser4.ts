// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Materialized via extractFn(internal `cb`) / export `knt`.
// npm identity: pdfjs-dist `SignatureExtractor`.

export {
  SignatureExtractor,
  SignatureExtractor as browser4,
} from "../vendor/pdfjs";

export type Browser4Peers = Record<string, unknown>;

/** @deprecated No-op; SignatureExtractor comes from pdfjs-dist. */
export function setBrowser4Peers(_next: Browser4Peers): void {}
