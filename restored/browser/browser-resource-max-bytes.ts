// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Soft ceiling for in-app browser resource payloads (`Cge`).

/** Bundle `Cge` (co-located with `Dge`) — 45 MiB. */
export const BROWSER_RESOURCE_MAX_BYTES = 45 * 1024 * 1024;
