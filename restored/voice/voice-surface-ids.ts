// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Avatar-overlay voice surface slot ids (bundle `Xge` / `$ht`).

export const VOICE_SURFACE_IDS = [
  "voice-microphone",
  "voice-controls",
  "voice-output",
] as const;

export type VoiceSurfaceId = (typeof VOICE_SURFACE_IDS)[number];
