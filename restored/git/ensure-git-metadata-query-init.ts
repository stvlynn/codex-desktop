// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `w3n` / export `B3` — empty __esm before git metadata watchForGitInit query keys.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureGitMetadataQueryInit(): void {}
