// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `xWo` / export `kD` — empty __esm before LocalGitActionsScope (commit/worktree-branch-setup).

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureLocalGitActionsScopeInit(): void {}
