// Restored from ref/webview/assets/pulsing-dot.module-CR6Hfk8C.js
// CSS-module class map for the pulsing-dot indicator (see pulsing-dot.module.css).

export type PulsingDotClasses = {
  pulseSize: string;
  pulsingDot: string;
  "pulsing-dot-pulse-size": string;
};

/**
 * Local class / keyframe names emitted for `pulsing-dot.module.css`.
 * Hashes match the bundled CSS asset `pulsing-dot-CGC7XOvr.css`.
 */
export const pulsingDotClasses: PulsingDotClasses = {
  pulseSize: "_pulseSize_6n73a_1",
  pulsingDot: "_pulsingDot_6n73a_2",
  "pulsing-dot-pulse-size": "_pulsing-dot-pulse-size_6n73a_1",
};

/** No-op Rolldown ESM init retained for graph compatibility. */
export function initPulsingDotModule(): void {}
