// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// pdf.js AnnotationMode enum (bundled copy). (bundle `Dy` / export `unt`).

export const PdfAnnotationMode = {
  DISABLE: 0,
  ENABLE: 1,
  ENABLE_FORMS: 2,
  ENABLE_STORAGE: 3,
} as const;
