// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// pdf.js PasswordResponses enum (bundled copy). (bundle `qKt` / export `wnt`).

export const PdfPasswordResponses = {
  NEED_PASSWORD: 1,
  INCORRECT_PASSWORD: 2,
} as const;
