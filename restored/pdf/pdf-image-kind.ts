// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// pdf.js ImageKind enum (bundled copy). (bundle `jy` / export `art`).

export const PdfImageKind = {
  GRAYSCALE_1BPP: 1,
  RGB_24BPP: 2,
  RGBA_32BPP: 3,
} as const;
