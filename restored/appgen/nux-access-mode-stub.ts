// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `X6s` / export `Bm` — observable body always returns `none`
// (NUX / auth queries are unused dead reads in the minified residual).

/** Bundle `Bm` — current residual always yields `none`. */
export function nuxAccessModeStub(): "none" {
  return "none";
}
