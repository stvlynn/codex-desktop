// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Settings route for creating a local environment (bundle `f5s`/`p5s` / export `hm`).

export const LOCAL_ENVIRONMENTS_CREATE_SEGMENT = "local-environments/create";

/** Full in-app settings path (bundle `p5s` / export `hm`). */
export const LOCAL_ENVIRONMENTS_CREATE_SETTINGS_PATH = `/settings/${LOCAL_ENVIRONMENTS_CREATE_SEGMENT}`;
