// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `fja` / export `ZN` — empty __esm before memory_scope / first_party_project_type helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})`). */
export function ensureMemoryScopeHelpersInit(): void {}
