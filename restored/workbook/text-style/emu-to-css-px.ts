// Restored from ref/webview/assets/workbook-C49Dgk1_.js
// Stage-3 wave-11: EMU → CSS-px factor (legacy workbookGt).

/** EMUs per CSS pixel at 96dpi (= points-per-EMU). Legacy workbookGt. */
export const EMU_TO_CSS_PX = 1 / 9525;

/** Legacy `workbookGt` alias. */
export const workbookGt = EMU_TO_CSS_PX;
