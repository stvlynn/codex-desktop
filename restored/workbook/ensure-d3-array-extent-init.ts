// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `LMt` / export `Iat` — fan-in of empty noop before d3-array extent / hypot helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})` or empty-noop fan-in). */
export function ensureD3ArrayExtentInit(): void {}
