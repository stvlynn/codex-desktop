// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `Uzt` / export `Hrt` — fan-in of empty noop before d3-shape stack order / normalize helpers.

/** No-op ensure-init (bundle body is empty `e(() => {})` or empty-noop fan-in). */
export function ensureD3ShapeStackOrderInit(): void {}
