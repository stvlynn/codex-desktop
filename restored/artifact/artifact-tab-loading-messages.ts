// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Artifact tab loading fallback copy (bundle `YWi` inside `eGi` / export `YV`).

import { defineMessages } from "react-intl";

export const artifactTabLoadingMessages = defineMessages({
  loadingPreview: {
    id: "artifactTab.loading",
    defaultMessage: "Loading preview...",
    description:
      "Loading state shown while an artifact tab preview is mounting",
  },
});
