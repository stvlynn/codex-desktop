// Restored from ref/webview/assets/app-initial-C-fROkKo.js
// Bundle `e8i` / export `YR`.

/** Euclidean distance between two points (annotation/pointer helpers). */
export function hypotDistance(
  x1: number,
  y1: number,
  x2: number,
  y2: number,
): number {
  return Math.hypot(x1 - x2, y1 - y2);
}
