// Restored from ref/webview/assets/PopcornElectronPresentationPanel-B28phSkk.js

// Wave5d soft stubs (qg unbound).
const _e: any = undefined;
const addPendingAnnotation: any = undefined;
const addPendingDrawing: any = undefined;
const annotationMode: any = undefined;
const anvilSecond: any = undefined;
const beaconSecond: any = undefined;
const boltSecond: any = undefined;
const controller: any = undefined;
const cragSecond: any = undefined;
const discSecond: any = undefined;
const dismissAnnotation: any = undefined;
const domeSecond: any = undefined;
const drawingCommitToken: any = undefined;
const drawingMode: any = undefined;
const fjordSecond: any = undefined;
const frameSecond: any = undefined;
const gearSecond: any = undefined;
const glenSecond: any = undefined;
const havenSecond: any = undefined;
const hearthSecond: any = undefined;
const hingeSecond: any = undefined;
const inletSecond: any = undefined;
const jadeiteSecond: any = undefined;
const kernelSecond: any = undefined;
const knobSecond: any = undefined;
const leafSecond: any = undefined;
const mapleSecond: any = undefined;
const mireSecond: any = undefined;
const rapidsSecond: any = undefined;
const reviewTools: any = undefined;
const spurSecond: any = undefined;
const torSecond: any = undefined;
const updatePendingAnnotation: any = undefined;
const updraftSecond: any = undefined;
const vergeSecond: any = undefined;
const vistaSecond: any = undefined;
// Wave FZ — full polished body from `PopcornElectronPresentationPanel-B28phSkk/auto-polished.tsx` with companion import rewrite.
// Soft-deferred host replaced; NOT app-initial extractFn / promote / producer barrel.
// Residual companion stubs: 5 (verified 106/111).
// Wave5d — FZ repair from clean staging/FZ; JSX PascalCase + careful split.
// Careful split 9/16
/* split-lane-import-depth:1 */

import type { ReactElement, ReactNode } from "react";

// Wave5d soft stubs.
const dew: any = undefined;
const ever: any = undefined;
const field: any = undefined;
const glen: any = undefined;
const nimbus: any = undefined;

import { ensureComposerEsm_Tft_Init } from "../../composer/composer-esm-inits";
import { react } from "../../boundaries/react-cjs-runtime";
import { workbookO, workbookS } from "../../workbook/editor-compat";
import { commonJsInit, esmInit } from "../../runtime/rolldown-runtime";
import {
  ensureDndAxisLockModifiersInit,
  restrictToVerticalAxis,
} from "../../ui/dnd-axis-lock-modifiers";
import { dndAxisLockModifiers } from "../../ui/dnd-axis-lock-modifiers-bindable";
import { useDndSensors } from "../../ui/use-dnd-sensors";
import { joinStringsOrNull as JoinStringsOrNull } from "../../utils/join-strings-or-null";
import { stack, stackOffsetExpand } from "../../vendor/d3-shape-line-pie-stack";
import { ensureD3ShapeStackOffsetInit } from "../../workbook/ensure-d3-shape-stack-offset-init";
import {
  popcornSurfaceStyle_B as PopcornSurfaceStyle_B,
  popcornSurfaceStyle_Ct as PopcornSurfaceStyle_Ct,
  popcornSurfaceStyle_dt,
  popcornSurfaceStyle_E,
  popcornSurfaceStyle_F,
  popcornSurfaceStyle_ft as PopcornSurfaceStyle_ft,
  popcornSurfaceStyle_G,
  popcornSurfaceStyle_H,
  popcornSurfaceStyle_I,
  popcornSurfaceStyle_it as PopcornSurfaceStyle_it,
  popcornSurfaceStyle_K as PopcornSurfaceStyle_K,
  popcornSurfaceStyle_L,
  popcornSurfaceStyle_lt,
  popcornSurfaceStyle_mt,
  popcornSurfaceStyle_N,
  popcornSurfaceStyle_O,
  popcornSurfaceStyle_P,
  popcornSurfaceStyle_r,
  popcornSurfaceStyle_R as PopcornSurfaceStyle_R,
  popcornSurfaceStyle_rt,
  popcornSurfaceStyle_st,
  popcornSurfaceStyle_t as PopcornSurfaceStyle_t,
  popcornSurfaceStyle_T,
  popcornSurfaceStyle_tt as PopcornSurfaceStyle_tt,
  popcornSurfaceStyle_Tt,
  popcornSurfaceStyle_U as PopcornSurfaceStyle_U,
  popcornSurfaceStyle_ut,
  popcornSurfaceStyle_V as PopcornSurfaceStyle_V,
  popcornSurfaceStyle_Y as PopcornSurfaceStyle_Y,
  popcornSurfaceStyle_Z as PopcornSurfaceStyle_Z,
} from "../popcorn-electron-surface-style";
import {
  ensurePopcornPageNumberNavigationInit,
  PopcornPageNumberNavigation,
} from "../popcorn-page-number-navigation";
import {
  RemoteTextEditSessionA,
  RemoteTextEditSessionC,
  RemoteTextEditSessionChrome,
  RemoteTextEditSessionI,
  RemoteTextEditSessionLowerB,
  RemoteTextEditSessionLowerG,
  RemoteTextEditSessionLowerH,
  RemoteTextEditSessionLowerR,
  RemoteTextEditSessionLowerV,
  RemoteTextEditSessionLowerW,
  RemoteTextEditSessionLowerX,
  RemoteTextEditSessionLowerY,
  RemoteTextEditSessionM,
  RemoteTextEditSessionN,
  RemoteTextEditSessionO,
  RemoteTextEditSessionS,
  RemoteTextEditSessionSurfaceKind,
  RemoteTextEditSessionUnderscore,
  RemoteTextEditSessionV,
  RemoteTextEditSessionW,
  RemoteTextEditSessionY,
  RemoteTextEditSessionZoomControl,
} from "../remote-text-edit-session/index";

function Vista__splitRest1(__splitParams: any) {
  let gasketSecond = glen.useCallback(() => {
      try {
        jadeiteSecond.current?.focus({
          preventScroll: true,
        });
      } catch {}
    }, []),
    handleSecond = glen.useCallback(() => {
      updraftSecond.current?.();
      updraftSecond.current = null;
      boltSecond(null);
    }, [boltSecond]),
    insertSecond = glen.useCallback(() => {
      torSecond.current = null;
      spurSecond.current = [];
      rapidsSecond([]);
    }, []),
    jacketSecond = glen.useCallback(() => {
      inletSecond.current = false;
      domeSecond(null);
      mireSecond(null);
      gasketSecond();
    }, [gasketSecond]),
    knurlSecond = glen.useCallback(() => {
      let diaphragmSecond = glenSecond.current;
      return diaphragmSecond?.mode === "create" &&
        diaphragmSecond.body.trim().length > 0
        ? inletSecond.current
          ? (jacketSecond(), true)
          : ((inletSecond.current = true),
            popcornSurfaceStyle_G(hearthSecond.current),
            false)
        : (jacketSecond(), true);
    }, [jacketSecond]),
    leverSecond = glen.useCallback(
      (elbowSecond) => {
        let ferruleSecond = gearSecond.slideRect;
        return ferruleSecond
          ? {
              x: ferruleSecond.left + elbowSecond.x * ferruleSecond.scale,
              y: ferruleSecond.top + elbowSecond.y * ferruleSecond.scale,
            }
          : {
              x: elbowSecond.x,
              y: elbowSecond.y,
            };
      },
      [gearSecond.slideRect],
    ),
    mountSecond = glen.useCallback(
      (grommetSecond) => {
        grommetSecond == null ||
          grommetSecond.points.length === 0 ||
          !beaconSecond ||
          !drawingMode ||
          ((torSecond.current = hingeSecond),
          (spurSecond.current = [...spurSecond.current, grommetSecond]),
          rapidsSecond(spurSecond.current));
      },
      [hingeSecond, beaconSecond, drawingMode],
    ),
    nozzleSecond = glen.useCallback(
      (headerSecond) => {
        if (headerSecond.length === 0) return;
        let injectorSecond = popcornSurfaceStyle_rt(headerSecond),
          jumperSecond = popcornSurfaceStyle_rt(
            headerSecond.map((item) => ({
              ...item,
              points: item.points.map(leverSecond),
            })),
          ),
          kerfSecond = jadeiteSecond.current,
          louverSecond = kernelSecond.current;
        if (
          !injectorSecond ||
          !jumperSecond ||
          !kerfSecond ||
          !louverSecond ||
          !gearSecond.slideRect
        )
          return;
        let manifoldSecond = {
            width: kerfSecond.clientWidth,
            height: kerfSecond.clientHeight,
          },
          nipSecond = headerSecond.reduce(
            (accumulator, current) =>
              Math.max(accumulator, current.strokeWidth),
            0,
          ),
          outletSecond = popcornSurfaceStyle_Tt(
            PopcornSurfaceStyle_t({
              left: jumperSecond.left - nipSecond / 2,
              top: jumperSecond.top - nipSecond / 2,
              width: jumperSecond.width + nipSecond,
              height: jumperSecond.height + nipSecond,
            }),
            manifoldSecond,
          ),
          packingSecond = {
            left: injectorSecond.left,
            top: injectorSecond.top,
            width: injectorSecond.width,
            height: injectorSecond.height,
          },
          reducerSecond = nimbus(
            havenSecond.selectedSlideElementTargets,
            packingSecond,
          ).flatMap((item) => {
            let wyeSecond = havenSecond.selectedSlideElementTargets.find(
              (_item) => _item.id === item,
            );
            return wyeSecond
              ? [
                  {
                    elementId: item,
                    frame: {
                      left: wyeSecond.frame.left,
                      top: wyeSecond.frame.top,
                      width: wyeSecond.frame.width,
                      height: wyeSecond.frame.height,
                      rotation: wyeSecond.frame.rotation,
                    },
                  },
                ]
              : [];
          }),
          strainerSecond = dew({
            snapshot: havenSecond,
            frame: packingSecond,
            viewportBounds: outletSecond,
            containedElements: reducerSecond,
          });
        if (!strainerSecond) return;
        let teeSecond = PopcornSurfaceStyle_Y("drawing"),
          unionSecond = field(strainerSecond);
        addPendingDrawing({
          drawingId: teeSecond,
          artifactKind: "presentation",
          label: unionSecond,
          target: strainerSecond,
          strokes: headerSecond,
        });
        let ventSecond = {
          x: outletSecond.left + outletSecond.width / 2,
          y: outletSecond.top + outletSecond.height / 2,
        };
        PopcornSurfaceStyle_t({
          cropRect: outletSecond,
          viewportSize: manifoldSecond,
          baseCanvas: louverSecond,
          overlayCanvases: [leafSecond.current, mapleSecond.current],
          strokes: headerSecond,
          projectPoint: leverSecond,
        })
          .then((value) => {
            let zenerSecond = {
                ...value,
                commentId: teeSecond,
              },
              alphaThird = {
                drawingId: teeSecond,
                artifactKind: "presentation",
                label: unionSecond,
                target: strainerSecond,
                strokes: headerSecond,
                screenshot: zenerSecond,
                browserCompatible: PopcornSurfaceStyle_t({
                  drawingId: teeSecond,
                  screenshot: zenerSecond,
                  markerViewportPoint: ventSecond,
                  viewportSize: manifoldSecond,
                }),
              };
            return reviewTools?.drawing?.onSubmit?.(alphaThird);
          })
          .catch((error) => {
            console.error(
              "Failed to capture presentation drawing screenshot",
              error,
            );
          });
      },
      [
        addPendingDrawing,
        leverSecond,
        reviewTools?.drawing,
        havenSecond,
        gearSecond.slideRect,
      ],
    ),
    platenSecond = (event) => {
      inletSecond.current = false;
      fjordSecond(event.annotationId);
      domeSecond({
        mode: "edit",
        annotationId: event.annotationId,
        target: event.target,
        body: event.body,
      });
    },
    ratchetSecond = (bravoThird = "saved", copperThird = "button") => {
      if (!cragSecond?.target) return;
      let deltaThird = cragSecond.body.trim();
      if (deltaThird.length === 0) return;
      if (cragSecond.mode === "edit") {
        updatePendingAnnotation(cragSecond.annotationId, {
          body: deltaThird,
        });
        reviewTools?.annotation?.onUpdate?.({
          annotationId: cragSecond.annotationId,
          artifactKind: "presentation",
          label: ever(cragSecond.target),
          body: deltaThird,
          target: cragSecond.target,
        });
        jacketSecond();
        return;
      }
      let echoThird = {
        annotationId: PopcornSurfaceStyle_Y("annotation"),
        artifactKind: "presentation",
        label: ever(cragSecond.target),
        body: deltaThird,
        target: cragSecond.target,
      };
      addPendingAnnotation(echoThird);
      bravoThird === "direct"
        ? (
            reviewTools?.annotation?.onDirectSubmit ??
            reviewTools?.annotation?.onSubmit
          )?.(echoThird)
        : reviewTools?.annotation?.onSubmit?.(echoThird);
      reviewTools?.annotation?.onSubmitted?.(
        echoThird,
        bravoThird,
        copperThird,
        {
          annotationMode,
        },
      );
      jacketSecond();
    },
    shimSecond = () => {
      cragSecond?.mode === "edit" &&
        (dismissAnnotation(cragSecond.annotationId),
        fjordSecond((falconThird) =>
          falconThird === cragSecond.annotationId ? null : falconThird,
        ),
        jacketSecond());
    };
  glen.useEffect(() => {
    anvilSecond || (cragSecond?.mode !== "edit" && domeSecond(null));
    (!anvilSecond || !annotationMode) &&
      (mireSecond(null), (knobSecond.current = null));
  }, [anvilSecond, cragSecond?.mode, annotationMode]);
  glen.useEffect(() => {
    if (beaconSecond && drawingMode) {
      domeSecond(null);
      mireSecond(null);
      knobSecond.current = null;
      _e(false);
      controller.setHoveredElementId(null);
      return;
    }
    let gammaThird = drawingCommitToken !== vergeSecond.current;
    handleSecond();
    spurSecond.current.length > 0 && !gammaThird && insertSecond();
  }, [
    handleSecond,
    insertSecond,
    controller,
    drawingCommitToken,
    beaconSecond,
    drawingMode,
  ]);
  glen.useEffect(() => {
    if (drawingCommitToken === vergeSecond.current) return;
    vergeSecond.current = drawingCommitToken;
    let harborThird = spurSecond.current;
    if (harborThird.length === 0) {
      insertSecond();
      return;
    }
    nozzleSecond(harborThird);
    insertSecond();
  }, [insertSecond, drawingCommitToken, nozzleSecond]);
  glen.useEffect(() => {
    if (!anvilSecond || !annotationMode) {
      _e(false);
      controller.setHoveredElementId(null);
      return;
    }
    _e(false);
    controller.setHoveredElementId(null);
  }, [anvilSecond, annotationMode, controller]);
  glen.useEffect(() => {
    cragSecond &&
      cragSecond.target.slideId !== hingeSecond &&
      (fjordSecond(null),
      mireSecond(null),
      (knobSecond.current = null),
      domeSecond(null));
  }, [cragSecond, hingeSecond]);
  glen.useEffect(
    () => () => {
      handleSecond();
    },
    [handleSecond],
  );
  glen.useEffect(() => {
    torSecond.current == null ||
      torSecond.current === hingeSecond ||
      insertSecond();
  }, [insertSecond, hingeSecond]);
  glen.useEffect(() => {
    let indigoThird = jadeiteSecond.current,
      jadeThird = mapleSecond.current,
      kiteThird =
        beaconSecond && drawingMode
          ? "crosshair"
          : anvilSecond && annotationMode && !cragSecond
            ? PopcornSurfaceStyle_t
            : "";
    return (
      indigoThird && (indigoThird.style.cursor = kiteThird),
      jadeThird && (jadeThird.style.cursor = kiteThird),
      () => {
        indigoThird &&
          indigoThird.style.cursor === kiteThird &&
          (indigoThird.style.cursor = "");
        jadeThird &&
          jadeThird.style.cursor === kiteThird &&
          (jadeThird.style.cursor = "");
      }
    );
  }, [cragSecond, anvilSecond, annotationMode, beaconSecond, drawingMode]);
  glen.useEffect(() => {
    if (!cragSecond) return;
    let lemonThird = (event) => {
      let marbleThird = event.target;
      marbleThird instanceof Node &&
        (hearthSecond.current?.contains(marbleThird) ||
          (marbleThird instanceof HTMLElement &&
            marbleThird.closest("[data-popcorn-annotation-marker='true']")) ||
          jadeiteSecond.current?.contains(marbleThird) ||
          knurlSecond());
    };
    return (
      document.addEventListener("mousedown", lemonThird, true),
      () => {
        document.removeEventListener("mousedown", lemonThird, true);
      }
    );
  }, [cragSecond, knurlSecond]);
  glen.useEffect(() => {
    let nickelThird = mapleSecond.current;
    if (!nickelThird) return;
    let onyxThird = discSecond.width,
      pearlThird = discSecond.height;
    if (onyxThird <= 0 || pearlThird <= 0) return;
    let quartzThird = window.devicePixelRatio || 1,
      riverThird = Math.max(1, Math.round(onyxThird * quartzThird)),
      slateThird = Math.max(1, Math.round(pearlThird * quartzThird));
    (nickelThird.width !== riverThird || nickelThird.height !== slateThird) &&
      ((nickelThird.width = riverThird), (nickelThird.height = slateThird));
    let timberThird = `${onyxThird}px`,
      umbraThird = `${pearlThird}px`;
    nickelThird.style.width !== timberThird &&
      (nickelThird.style.width = timberThird);
    nickelThird.style.height !== umbraThird &&
      (nickelThird.style.height = umbraThird);
    let violetThird = nickelThird.getContext("2d");
    violetThird &&
      (violetThird.setTransform(quartzThird, 0, 0, quartzThird, 0, 0),
      violetThird.clearRect(0, 0, onyxThird, pearlThird),
      gearSecond.slideRect &&
        havenSecond.selectedSlideFrame &&
        (violetThird.save(),
        violetThird.translate(
          gearSecond.slideRect.left,
          gearSecond.slideRect.top,
        ),
        violetThird.scale(
          gearSecond.slideRect.scale,
          gearSecond.slideRect.scale,
        ),
        frameSecond?.drawOverlay(violetThird),
        violetThird.restore()));
  }, [
    discSecond.height,
    discSecond.width,
    vistaSecond,
    gearSecond.slideRect,
    havenSecond.selectedSlideFrame,
    havenSecond.textEditState,
    havenSecond.textLayoutBlocks,
    frameSecond,
  ]);
  return undefined as any;
}
