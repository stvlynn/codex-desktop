import type { OAILightingSide } from "./oai_lighting_side";
/**
 * Device lighting configuration for this integration. `ambient` drives the outer
 * ring; `keys` drives the lighting under the keycaps.
 */
export interface OAILightingConfig {
    ambient: OAILightingSide;
    keys: OAILightingSide;
}
//# sourceMappingURL=oai_lighting_config.d.ts.map