import type { OAILightingEffect } from "./oai_lighting_effect";
/**
 * Lighting state for one UI thread on the device.
 *
 * **Only `id` is required.** Every other property is optional; omit a property when
 * you want that aspect of the thread to stay unchanged.
 *
 * | Field | Description |
 * |---|---|
 * | `id` | Which thread to update. |
 * | `color` | Packed RGB color (integer). |
 * | `brightness` | Intensity from 0 (off) through 1 (full on). |
 * | `effect` | Active animation effect. |
 * | `speed` | Effect speed from 0 (stopped) through 1 (fast). |
 * | `syncKeysLighting` | When set, keys backlight follows this thread's RGB. |
 * | `syncAmbientLighting` | When set, ambient lighting follows this thread's RGB. |
 */
export interface ThreadLighting {
    id: number;
    color?: number;
    brightness?: number;
    effect?: OAILightingEffect;
    speed?: number;
    syncKeysLighting?: boolean;
    syncAmbientLighting?: boolean;
}
//# sourceMappingURL=thread_lighting.d.ts.map