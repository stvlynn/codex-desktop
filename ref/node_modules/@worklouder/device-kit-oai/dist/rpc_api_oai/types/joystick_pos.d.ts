/**
 * Joystick movement payload emitted by the device.
 */
export interface JoystickPos {
    /** Joystick angle, normalized from 0 to 1. */
    angle: number;
    /** Joystick displacement from center, normalized from 0 to 1. */
    distance: number;
}
//# sourceMappingURL=joystick_pos.d.ts.map