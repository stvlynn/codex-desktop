/**
 * Built-in LED animation effects supported by Work Louder device firmware.
 * Used for the ambient ring, keys backlight, and per-thread lighting on OpenAI
 * integration builds.
 */
export declare enum OAILightingEffect {
    /** LEDs are turned off. */
    off = 0,
    /** All LEDs show a single static color. */
    solid = 1,
    /** A colored segment travels across the LED strip. */
    snake = 2,
    /** Continuously cycles through the full hue spectrum. */
    rainbow = 3,
    /** LEDs fade in and out in a breathing pattern. */
    breath = 4,
    /** A smooth color gradient spans the LED strip. */
    gradient = 5,
    /** LEDs fade in and out in a breathing pattern. Starts at 0.5 brighness and goes to 1 */
    shallowBreath = 6
}
//# sourceMappingURL=oai_lighting_effect.d.ts.map