import type { OAILightingEffect } from "./oai_lighting_effect";
/**
 * Settings for one lighting zone on the device: animation effect,
 * brightness, speed, magic parameter, and packed RGB color. Brightness and speed
 * are normalized (0–1), matching `SideLightingConfig` in the base device kit.
 */
export interface OAILightingSide {
    effect: OAILightingEffect;
    brightness: number;
    speed: number;
    magic: number;
    color: number;
}
//# sourceMappingURL=oai_lighting_side.d.ts.map