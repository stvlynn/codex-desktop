/**
 * Payload for HID key notifications emitted by the device.
 */
export interface HIDKey {
    /** HID key identifier. */
    key: string;
    /** Optional action type (for example, press or release). */
    act?: number;
    /** Optional agent index that triggered the event. */
    agent?: number;
}
//# sourceMappingURL=hid_key.d.ts.map