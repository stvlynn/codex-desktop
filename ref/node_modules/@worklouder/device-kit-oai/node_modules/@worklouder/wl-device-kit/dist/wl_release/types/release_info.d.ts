/**
 * Describes a resolved release that is ready for download.
 */
export interface ReleaseInfo {
    /** Semver version string of the release. */
    version: string;
    /** Direct URL to the downloadable binary. */
    downloadUrl: string;
    /** Markdown-formatted changelog body. */
    changeLog: string;
    /** Unix timestamp (ms) of when the release info was fetched. */
    fetchedAt: number;
}
//# sourceMappingURL=release_info.d.ts.map