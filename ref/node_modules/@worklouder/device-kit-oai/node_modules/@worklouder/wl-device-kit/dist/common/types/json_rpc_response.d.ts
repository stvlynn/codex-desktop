/**
 * A successful JSON-RPC 2.0 response received from a Work Louder device.
 */
export interface JsonRpcResponse {
    /** The result payload returned by the device. Structure varies by method. */
    result: any;
    /** The request identifier this response corresponds to, or `null` for notifications. */
    id: string | number | null;
}
/**
 * A JSON-RPC 2.0 error response received from a Work Louder device.
 */
export interface JsonRpcError {
    /** The error object describing what went wrong. */
    error: {
        /** Machine-readable error code. */
        code: number;
        /** Human-readable error description. */
        message: string;
        /** Optional additional error data. */
        data?: any;
    };
    /** The request identifier this error corresponds to, or `null` for notifications. */
    id: string | number | null;
}
//# sourceMappingURL=json_rpc_response.d.ts.map