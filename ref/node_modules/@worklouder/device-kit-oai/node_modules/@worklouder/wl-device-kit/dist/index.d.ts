/**
 * `wl-device-kit` — Work Louder device communication library.
 *
 * This package provides everything needed to discover, connect to, and
 * interact with Work Louder hardware (keyboards, macro pads, knobs) over
 * USB HID and serial.
 *
 * **Main entry points:**
 *
 * | Class | Purpose |
 * |---|---|
 * | {@link WLDeviceDiscovery} | Scan USB for connected Work Louder devices |
 * | {@link WLDeviceCommImpl}  | Open a transport connection (HID / serial) |
 * | {@link WLRPCApi}          | High-level typed RPC methods (status, files, lighting, …) |
 * | {@link WLRPCClient}       | Low-level JSON-RPC 2.0 request/response client |
 *
 * @example
 * ```ts
 * import {
 *   WLDeviceDiscovery,
 *   WLDeviceCommImpl,
 *   WLRPCApi,
 * } from "@worklouder/wl-device-kit";
 *
 * const discovery = new WLDeviceDiscovery();
 * const [device] = discovery.findWLDevices();
 *
 * const comm = new WLDeviceCommImpl();
 * await comm.connect(device);
 *
 * const api = new WLRPCApi(comm);
 * const status = await api.getDeviceStatus();
 * console.log(status.firmwareVersion);
 *
 * await comm.disconnect();
 * ```
 *
 * @packageDocumentation
 */
export * from "./wl_rpc_api";
export * from "./wl_device_discovery";
export * from "./wl_device_comm";
export * from "./wl_rpc_client";
export * from "./wl_lvgl";
export * from "./wl_device_programmer";
export * from "./wl_permissions";
export * from "./wl_release";
export type { WLDevice } from "./common/types/wl_device";
export { ConnectionType } from "./common/types/connection_type";
export { DeviceType } from "./common/types/device_type";
export { DeviceLayoutType } from "./common/types/device_layout_type";
export type { JsonRpcRequest } from "./common/types/json_rpc_request";
export type { JsonRpcResponse, JsonRpcError } from "./common/types/json_rpc_response";
export { type Logger, noopLogger } from "./common/types/logger";
export { WLDeviceError, WLDeviceErrorCode } from "./common/types/wl_device_error";
//# sourceMappingURL=index.d.ts.map