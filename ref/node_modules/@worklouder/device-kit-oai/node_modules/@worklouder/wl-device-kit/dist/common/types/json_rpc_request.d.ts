/**
 * A JSON-RPC 2.0 request object sent to a Work Louder device.
 */
export interface JsonRpcRequest {
    /** The RPC method name to invoke on the device (e.g. `"sys.version"`, `"fs.read"`). */
    method: string;
    /** Optional parameters for the method. Structure depends on the method being called. */
    params?: any;
    /** Unique request identifier used to correlate responses. */
    id: number | string;
}
//# sourceMappingURL=json_rpc_request.d.ts.map