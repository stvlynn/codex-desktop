import { FwApp } from "./fw_app";
/**
 * Firmware app that is currently available in the device app carousel.
 *
 * Native apps are built into firmware; non-native apps are custom apps
 * installed on the device filesystem.
 */
export interface FwActiveApp extends FwApp {
    /** Unique app bundle identifier (for example, `"cc.worklouder.home"`). */
    bundleId: string;
    /** Human-readable app name shown by the device. */
    name: string;
    /** `true` when the app is built into firmware, `false` for a custom installed app. */
    native: boolean;
}
//# sourceMappingURL=fw_active_app.d.ts.map