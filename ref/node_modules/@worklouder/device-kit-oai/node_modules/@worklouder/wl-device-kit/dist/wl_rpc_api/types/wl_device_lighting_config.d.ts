import { SideLightingConfig } from "./side_lighting_config";
/**
 * Complete lighting configuration for a Work Louder device, covering
 * both the key backlight and the underglow LED strip.
 *
 * Sent to the device via {@link WLRPCApi.sendLightingPreview} to apply
 * a real-time lighting preview without persisting to flash.
 */
export interface WLDeviceLightingConfig {
    /** Configuration for the per-key backlight LEDs. */
    backlight: SideLightingConfig;
    /** Configuration for the underglow (bottom-facing) LED strip. */
    underglow: SideLightingConfig;
}
//# sourceMappingURL=wl_device_lighting_config.d.ts.map