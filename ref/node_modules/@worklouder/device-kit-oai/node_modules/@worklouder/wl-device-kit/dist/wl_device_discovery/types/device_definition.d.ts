import { DeviceLayoutType } from "../../common/types/device_layout_type";
import { DeviceType } from "../../common/types/device_type";
/**
 * Maps a USB Product ID to its corresponding device model and layout.
 * Used internally by the {@link DEVICE_REGISTRY} lookup table.
 */
export interface DeviceDefinition {
    /** The Work Louder product model this PID represents. */
    type: DeviceType;
    /** The keyboard layout variant shipped with this PID. */
    layout: DeviceLayoutType;
}
//# sourceMappingURL=device_definition.d.ts.map