/**
 * Summary of a single file stored on the device's filesystem,
 * returned by {@link WLRPCApi.getFileList}.
 */
export interface WLDeviceFileSummary {
    /** Filename (without directory path). */
    name: string;
    /** File size in bytes. */
    size: number;
    /** Optional checksum (e.g. CRC or hash) computed by the device firmware. */
    checksum?: string;
}
//# sourceMappingURL=wl_device_file_summary.d.ts.map