/**
 * Describes the currently focused desktop application, sent to the device
 * via the `host.focused_app` RPC so it can display context-aware UI.
 */
export interface AppInFocus {
    /** Human-readable application name (e.g. `"Google Chrome"`). */
    appName?: string;
    /** Process / executable name (e.g. `"chrome"`). */
    process?: string;
    /** Filesystem path to the application bundle or executable. */
    path?: string;
}
//# sourceMappingURL=app_in_focus.d.ts.map