import { ConnectionEventType } from "./connection_event_type";
/**
 * Discriminated union emitted by {@link WLDeviceComm.onConnectionEvent}
 * whenever the transport-layer state of a device connection changes.
 *
 * Use the `type` field to narrow the variant:
 *
 * - `CONNECTED` – the device link is up and ready for RPC traffic.
 * - `DISCONNECTED` – the device link was closed (cable unplugged, port closed, etc.).
 * - `ERROR` – an unrecoverable transport error occurred; inspect the `error` field for details.
 */
export type ConnectionEvent = {
    type: ConnectionEventType.CONNECTED;
} | {
    type: ConnectionEventType.DISCONNECTED;
} | {
    type: ConnectionEventType.ERROR;
    error: any;
};
//# sourceMappingURL=connection_event.d.ts.map