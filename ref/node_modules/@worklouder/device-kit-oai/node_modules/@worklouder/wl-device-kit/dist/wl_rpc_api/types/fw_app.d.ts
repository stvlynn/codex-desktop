/**
 * Custom firmware app installed on a Work Louder device.
 */
export interface FwApp {
    /** Unique app bundle identifier (for example, `"cc.worklouder.timer"`). */
    bundleId: string;
    /** Human-readable app name shown by the device. */
    name: string;
}
//# sourceMappingURL=fw_app.d.ts.map