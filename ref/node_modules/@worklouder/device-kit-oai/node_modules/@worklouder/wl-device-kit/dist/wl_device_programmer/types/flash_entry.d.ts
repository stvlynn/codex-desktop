export interface FlashEntry {
    /** Memory address at which to flash the file (e.g. 0 for a single merged binary). */
    address: number;
    /** Filesystem path to the firmware binary. */
    path: string;
}
//# sourceMappingURL=flash_entry.d.ts.map