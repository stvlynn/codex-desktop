/**
 * Runtime status snapshot of a connected Work Louder device,
 * returned by {@link WLRPCApi.getDeviceStatus}.
 */
export interface WLDeviceStatus {
    /** Semantic version string of the running firmware (e.g. `"1.2.3"`). */
    firmwareVersion?: string;
    /** Zero-based index of the currently active profile. */
    selectedProfileIndex?: number;
    /** Zero-based index of the currently active layer within the selected profile. */
    selectedLayerIndex?: number;
    /** Battery charge level as a percentage (0–100). Only present on wireless-capable devices. */
    batteryPercentage?: number;
    /** `true` if the device is currently charging via USB. */
    isCharging?: boolean;
}
//# sourceMappingURL=wl_device_status.d.ts.map