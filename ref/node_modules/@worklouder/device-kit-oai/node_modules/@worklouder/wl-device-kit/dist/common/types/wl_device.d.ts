import { ConnectionType } from "./connection_type";
import { DeviceLayoutType } from "./device_layout_type";
import { DeviceType } from "./device_type";
/**
 * Describes a discovered Work Louder device with the information needed to
 * open a connection and identify its capabilities.
 *
 * Returned by {@link WLDeviceDiscovery.findWLDevices} and
 * {@link WLDeviceDiscovery.findWLBootloaderDevices}, and passed to
 * {@link WLDeviceComm.connect} to establish communication.
 */
export interface WLDevice {
    /** OS-level path used to open the HID or serial port (e.g. a HID device path or COM port). */
    portPath: string;
    /** USB Product ID of the device, as a decimal string. */
    devicePid: string;
    /** Transport type for this device (HID or serial). */
    connectionType: ConnectionType;
    /** The Work Louder product model. */
    deviceType: DeviceType;
    /** Physical keyboard layout variant. */
    layoutType: DeviceLayoutType;
    /** `true` when the device is connected over a wired USB cable (as opposed to wireless). */
    isUsbConnection: boolean;
}
//# sourceMappingURL=wl_device.d.ts.map