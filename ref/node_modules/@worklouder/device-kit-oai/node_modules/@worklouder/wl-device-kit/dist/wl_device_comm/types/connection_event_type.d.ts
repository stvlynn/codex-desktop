/**
 * Discriminator for {@link ConnectionEvent} variants.
 */
export declare enum ConnectionEventType {
    /** The device connection was successfully established. */
    CONNECTED = 0,
    /** The device was disconnected (port closed). */
    DISCONNECTED = 1,
    /** A transport-level error occurred on the connection. */
    ERROR = 2
}
//# sourceMappingURL=connection_event_type.d.ts.map