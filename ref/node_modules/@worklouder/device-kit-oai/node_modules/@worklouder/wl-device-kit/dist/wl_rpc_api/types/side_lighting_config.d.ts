import { LightingEffect } from "./lighting_effect";
/**
 * Configuration for a single lighting zone (backlight or underglow).
 * Passed as part of {@link WLDeviceLightingConfig} to
 * {@link WLRPCApi.sendLightingPreview}.
 */
export interface SideLightingConfig {
    /** The animation effect to apply. */
    effect: LightingEffect;
    /** Intensity, normalized 0–1. */
    brightness?: number;
    /** Effect speed, normalized 0–1. */
    speed?: number;
    /** Effect-specific parameter, normalized 0–1. */
    magic?: number;
    /** Packed RGB color as a 32-bit integer (e.g. `0xFF0000` for red). */
    color?: number;
}
//# sourceMappingURL=side_lighting_config.d.ts.map